# 《基于无人机载平台的卫星互联网终端高精度定位方法》逐页答辩讲稿

> 使用建议：每页按“先讲问题，再讲方法，再讲结果或过渡”的节奏控制。公式页不要逐字念公式，重点解释“变量含义、为什么这样建模、公式服务什么目标”。

> 答辩前提醒：第 15 页和第 17 页关于 DQN 输出维度建议统一。根据动作集合为 9 类增量调权动作，DQN 输出应为 9 个动作的 Q 值；如果图中仍写成 6 维输出，建议答辩 PPT 中改为 `Linear(64,9)`。

## 第 1 页：题目页

各位老师好，我是石志鹏，我的论文题目是《基于无人机载平台的卫星互联网终端高精度定位方法》。本论文面向复杂城市环境下的卫星互联网终端定位问题，研究如何利用无人机载平台获取空间观测信息，并在遮挡、多径、非视距和节点缺失等不利条件下提升定位精度与鲁棒性。

我的工作主要包含两个部分：第一部分是面向单目标连续定位的物理感知深度强化学习自适应融合方法；第二部分是面向多目标并发定位的空间信号表征与协同鲁棒定位方法。下面我按照研究背景、方法设计和实验验证三个层次进行汇报。

## 第 2 页：汇报提纲

本次答辩分为三个部分。第一部分介绍研究背景，说明为什么卫星互联网终端定位在复杂城市环境中具有研究价值，以及传统定位方法面临哪些困难。

第二部分介绍单目标自适应融合定位方法。这里的核心问题是：RSSI、AOA 和 PDR 等不同观测源在不同传播环境下可靠性会动态变化，因此需要一种能够随环境变化自动调整融合权重的方法。

第三部分介绍多目标协同鲁棒定位方法。这里进一步考虑多个终端并发存在，以及多无人机节点随机掉点造成观测残缺的问题，重点解决复杂场景下区域级定位的稳定性。

## 第 3 页：第一部分过渡页

首先进入研究背景部分。本论文关注的是复杂城市环境下卫星互联网终端的高精度定位需求。这里的“复杂”主要体现在建筑密集、遮挡严重、多径丰富以及终端可能处于非合作或弱合作状态，导致传统单一观测方式很难稳定获得高精度结果。

## 第 4 页：研究背景与意义

随着天地一体化网络的发展，低轨卫星互联网正在快速部署，终端数量和应用场景都在增加。卫星互联网不仅服务通信，也逐渐成为态势感知、应急通信和空间信息服务的重要基础设施。因此，在城市环境中对卫星互联网终端进行快速、准确定位，具有明显的工程和安全意义。

但是，卫星通信终端定位与传统 GNSS 定位不同。很多场景下终端并不主动提供位置信息，或者定位环境中存在强遮挡和多径。传统方法通常依赖接收信号强度、到达角、到达时间等物理观测量，但这些观测量在城市峡谷环境中会受到环境变化影响。例如 RSSI 容易受路径损耗和遮挡影响，AOA 容易被反射路径偏置，时间类测量又依赖精确同步。

因此，本论文的出发点是：利用无人机平台的机动性，在空间中主动获取多源观测，并结合物理建模与智能学习方法，提升复杂传播环境下终端定位的精度和鲁棒性。

## 第 5 页：第二部分过渡页

接下来介绍第一项工作：基于物理感知深度强化学习的单目标自适应融合定位方法。这个部分解决的是单个目标终端的连续定位问题。

核心问题可以概括为一句话：在复杂传播环境下，RSSI、AOA、PDR 三类定位结果的可信度不是固定的，如何根据当前环境状态动态调整它们在融合定位中的权重。

## 第 6 页：任务场景与模型

这一页给出了单目标定位任务场景。目标终端位置未知，无人机在目标区域上方或周边进行观测，获得 RSSI、AOA、时间特征等信号信息，同时结合目标或平台的运动递推信息形成 PDR 估计。

从建模角度看，每个时刻系统都包含目标状态、无人机观测位置、传播环境和观测噪声。由于无人机可以在不同空间位置采样，因此它相比地面固定节点具有更强的观测灵活性。但是这种灵活性也带来一个问题：不同观测位置对应的传播条件差异很大，单一观测模型很难在所有时刻都可靠。

所以本部分不是只依赖某一种定位源，而是先分别得到 RSSI 定位、AOA 定位和 PDR 递推定位结果，然后通过自适应融合得到最终位置。

## 第 7 页：复杂环境下的观测退化

这一页解释为什么需要多源融合。RSSI 的优点是容易获取，不需要复杂硬件，但它受路径损耗、遮挡和阴影衰落影响较大。AOA 的优点是具有几何方向约束，但在反射路径较强时，接收到的主路径方向可能不是直达方向，因此会产生角度偏置。TOA 或 TDOA 理论上可以提供距离约束，但在非合作终端场景中，时钟同步和发射时间未知会显著放大误差。PDR 的优点是短时间内轨迹平滑，但误差会随时间累积，长期会产生漂移。

因此，每一种观测都有适用条件，也都有失效场景。固定权重融合的假设是“每个观测源长期可靠性不变”，但在复杂城市环境中这个假设并不成立。本论文第一项工作的目标，就是让融合权重能够跟随物理环境和观测质量动态变化。

## 第 8 页：任务场景与数学模型

这一页把定位问题形式化。目标终端二维位置表示为：

$$
p_t=[x_t,y_t]^T
$$

其中 \(x_t\) 和 \(y_t\) 是目标在地面平面上的坐标。第 \(m\) 个无人机观测节点的位置表示为：

$$
s_{m,t}=[x_{m,t},y_{m,t},z_{m,t}]^T
$$

这里多了 \(z_{m,t}\)，是因为无人机在三维空间中观测，具有飞行高度。目标与无人机之间的几何距离为：

$$
d_{m,t}=\sqrt{(x_t-x_{m,t})^2+(y_t-y_{m,t})^2+z_{m,t}^2}
$$

这个公式说明，虽然目标定位结果通常输出二维地面坐标，但信号传播过程是三维的，距离和遮挡都会受到无人机高度影响。

观测模型写成：

$$
z_{m,t}=h_m(p_t,s_{m,t};\xi_t)+v_{m,t}
$$

其中 \(z_{m,t}\) 是第 \(m\) 个节点在时刻 \(t\) 得到的观测量，\(h_m(\cdot)\) 是由目标位置、无人机位置和传播环境共同决定的观测函数，\(\xi_t\) 表示当前环境因素，\(v_{m,t}\) 表示噪声。这个模型的重点是：观测误差不仅来自随机噪声，也来自传播环境变化，因此定位需要具备环境自适应能力。

## 第 9 页：融合模型总体框架

这一页是第一项方法的总体框架。整个系统分为三层：第一层是多源定位分支，分别由 RSSI、AOA 和 PDR 得到三个候选位置；第二层是物理感知特征提取，提取 RMS 时延扩展、RSSI 方差、RF 与 PDR 的几何一致性以及上一时刻融合权重；第三层是 PA-DQN 智能体，根据这些状态特征输出调权动作。

最终融合中心根据 DQN 给出的动作更新权重，得到融合定位结果 \(P_t^{Fused}\)。训练阶段使用真实位置 \(P_t^{GT}\) 计算误差，并把误差反馈为奖励，从而让智能体学习“什么环境下应该相信哪一种观测源”。

这里的创新点不是简单把神经网络用于回归位置，而是把神经网络用于“融合决策”。也就是说，网络不直接输出坐标，而是输出调权动作的价值，进而控制多源定位结果的融合权重。

## 第 10 页：多源加权融合模型

这一页给出三个定位分支和最终融合公式。RSSI 分支的观测模型为：

$$
r_{m,t}=P_0-10\eta\log_{10}\frac{d_{m,t}}{d_0}+b_{m,t}^{NLOS}+v^r_{m,t}
$$

其中 \(r_{m,t}\) 是接收信号强度，\(P_0\) 是参考距离 \(d_0\) 处的接收功率，\(\eta\) 是路径损耗指数，\(d_{m,t}\) 是目标和无人机之间的距离，\(b_{m,t}^{NLOS}\) 表示非视距带来的系统偏差，\(v^r_{m,t}\) 是随机噪声。RSSI 定位本质上是通过信号强度反推距离，再估计目标位置。

AOA 分支的角度观测为：

$$
\theta^{AOA}_{m,t}=\operatorname{atan2}(y_t-y_{m,t},x_t-x_{m,t})+b^\theta_{m,t}+v^\theta_{m,t}
$$

其中 \(\operatorname{atan2}\) 给出目标相对于无人机的真实方位角，\(b^\theta_{m,t}\) 是反射或非视距造成的角度偏置，\(v^\theta_{m,t}\) 是角度噪声。AOA 提供的是方向约束。

PDR 分支采用递推模型：

$$
P_t^{PDR}=P_{t-1}^{PDR}+
\begin{bmatrix}
\hat l_t\cos\hat\psi_t\\
\hat l_t\sin\hat\psi_t
\end{bmatrix}
+w_t
$$

其中 \(\hat l_t\) 是估计步长或位移长度，\(\hat\psi_t\) 是运动航向角，\(w_t\) 是递推噪声。PDR 的特点是短时连续性好，但长期会积累漂移。

最终融合公式为：

$$
P_t^{Fused}=\omega_t^{RSSI}P_t^{RSSI}
+\omega_t^{AOA}P_t^{AOA}
+\omega_t^{PDR}P_t^{PDR}
$$

其中 \(\omega_t^{RSSI}\)、\(\omega_t^{AOA}\)、\(\omega_t^{PDR}\) 是三个定位源的权重，满足非负且和为 1。本文的重点就是让这些权重不再固定，而是由强化学习根据当前物理状态动态调整。

## 第 11 页：基于 Q-learning 的 RLIFA 融合方法

这一页先介绍 Q-learning 的基本思想，用它作为 DQN 方法的前置基础。强化学习把定位融合看成一个序贯决策问题：每一时刻系统处于状态 \(s_t\)，智能体选择一个动作 \(a_t\)，也就是调整融合权重；系统给出奖励 \(r_t\)，通常与定位误差有关；然后进入下一状态。

累计回报定义为：

$$
G_t=\sum_{k=0}^{\infty}\gamma^k r_{t+k},\quad 0\le\gamma<1
$$

这里 \(G_t\) 不是只看当前奖励，而是把当前之后所有奖励按折扣系数 \(\gamma\) 加权求和。 \(\gamma\) 越接近 1，说明越重视长期效果；越接近 0，说明越重视当前一步的效果。

策略目标是：

$$
\pi^*(s)=\arg\max_{\pi}E_{\pi}[G_t|s_t=s]
$$

意思是，在状态 \(s\) 下选择能够最大化期望长期回报的策略。动作价值函数定义为：

$$
Q^\pi(s,a)=E_\pi[G_t|s_t=s,a_t=a]
$$

Q 值表示“在状态 \(s\) 下执行动作 \(a\)，之后按照策略继续行动，最终能获得多少长期收益”。所以 Q 值越大，说明这个动作越值得选择。

最优 Q 值满足贝尔曼最优方程：

$$
Q^*(s,a)=E[r_t+\gamma\max_{a'}Q^*(s_{t+1},a')|s_t=s,a_t=a]
$$

它的含义是：当前动作的价值等于“当前奖励”加上“下一状态中最优动作的折扣价值”。这就是 Q-learning 更新的理论基础。

## 第 12 页：RLIFA 状态与动作设计

这一页给出 Q-learning 版本的状态和动作设计。状态由当前融合定位误差离散化得到：

$$
\epsilon_t=\|P_t^{Fused}-P_t^{GT}\|_2
$$

其中 \(\epsilon_t\) 是融合位置与真实位置之间的欧氏距离。为了构造 Q 表，把连续误差映射为离散状态：

$$
s_t=
\begin{cases}
\operatorname{round}(100\epsilon_t),&0\le\epsilon_t<1\\
100,&\epsilon_t\ge1
\end{cases}
$$

也就是说，如果误差小于 1 米，就把它乘以 100 后四舍五入，形成 0 到 99 的细粒度状态；如果误差大于等于 1 米，就统一映射到状态 100。这样 Q 表的状态空间为 101 个状态。

动作集合是 9 类增量式调权动作。具体来说，RSSI 权重和 AOA 权重分别可以增加、保持或减少，因此两者组合形成 \(3\times 3=9\) 个动作；PDR 权重由归一化约束自动得到。这样设计的好处是动作空间较小，并且每次只做小幅权重调整，避免融合结果剧烈跳变。

## 第 13 页：RLIFA 奖励、Q 表更新与参数

这一页是 Q-learning 的核心更新过程。奖励函数根据定位误差设计：

$$
r_k=
\begin{cases}
100,& e_k=0\\
\operatorname{round}(100/e_k),&0<e_k<1\\
-100,&e_k\ge1
\end{cases}
$$

这个奖励的逻辑是：误差越小，奖励越大；误差超过 1 米时给负奖励，促使智能体避免较差的权重组合。它把定位精度直接转化为强化学习的优化目标。

Q 表更新公式为：

$$
Q(s_t,a_t)\leftarrow Q(s_t,a_t)+
\alpha\left[r_t+\gamma\max_a Q(s_{t+1},a)-Q(s_t,a_t)\right]
$$

括号中的部分叫 TD 误差，也就是“新的目标价值”和“当前估计价值”的差。 \(\alpha\) 是学习率，控制每次更新幅度；\(\gamma\) 是折扣因子，控制未来奖励的重要性；\(\max_a Q(s_{t+1},a)\) 表示下一状态中最优动作的价值。

动作选择采用 \(\epsilon\)-greedy 策略：

$$
a_t=
\begin{cases}
\text{随机动作},&\text{概率 }\epsilon\\
\arg\max_a Q(s_t,a),&\text{概率 }1-\epsilon
\end{cases}
$$

它的作用是平衡探索和利用。训练初期需要多探索不同调权方式，训练后期更多选择当前 Q 值最大的动作。

## 第 14 页：从 Q-learning 升级到 DQN

Q-learning 的问题在于它依赖 Q 表。当状态很简单时，Q 表可行；但如果要引入 RMS 时延扩展、RSSI 方差、几何一致性、历史权重等连续物理特征，状态空间会变得高维且连续，Q 表无法有效存储和泛化。

因此本文引入 DQN，也就是深度 Q 网络。DQN 的核心思想是用神经网络近似 Q 函数：

$$
Q(S_t,a_t)\approx Q(S_t,a_t;\theta)
$$

其中 \(S_t\) 是连续状态向量，\(\theta\) 是神经网络参数。这样即使没有见过完全相同的状态，网络也可以根据相似特征进行泛化，输出各个动作的价值。

所以从 RLIFA 到 PA-DQN 的升级点在于：状态不再只是误差离散编号，而是包含物理环境感知信息；决策不再依赖查表，而是由神经网络根据连续状态输出动作价值。

## 第 15 页：DQN 动作价值函数的网络近似

这一页说明 PA-DQN 的输入和输出。输入状态定义为：

$$
S_t=
[\sigma_{\tau,t},\sigma^2_{RSSI,t},D_{RF-PDR,t},
\omega_{t-1}^{RSSI},\omega_{t-1}^{AOA},\omega_{t-1}^{PDR}]^T
\in R^6
$$

这里一共有 6 个维度。 \(\sigma_{\tau,t}\) 是 RMS 时延扩展，用来表征多径程度；\(\sigma^2_{RSSI,t}\) 是 RSSI 方差，用来反映信号波动强弱；\(D_{RF-PDR,t}\) 是 RF 定位与 PDR 递推之间的几何一致性差异，用来判断不同定位源是否相互矛盾；后三项是上一时刻三个融合权重，表示当前权重状态和历史惯性。

DQN 的输出不是位置，也不是连续权重，而是每个离散动作对应的 Q 值：

$$
Q(S_t;\theta)=
[Q(S_t,a_1;\theta),Q(S_t,a_2;\theta),\cdots,Q(S_t,a_9;\theta)]^T
$$

由于动作集合为 9 类增量式调权动作，所以输出应为 9 维动作价值向量。在线推理时选择 Q 值最大的动作：

$$
a_t=\arg\max_{a_i\in A}Q(S_t,a_i;\theta)
$$

这里 Q 值越大，表示在当前物理状态下选择该调权动作的长期收益越高，也就是更可能带来更小的定位误差和更稳定的融合过程。

## 第 16 页：DQN 训练目标与在线调权流程

这一页是 PA-DQN 的训练核心。首先是奖励函数：

$$
r_t=\exp(-k\|P_{GT}-P_{Fused}\|_2)-\lambda\|\omega_t-\omega_{t-1}\|_2
$$

第一项 \(\exp(-k\|P_{GT}-P_{Fused}\|_2)\) 是精度奖励。定位误差越小，指数项越接近 1；误差越大，指数项越接近 0。参数 \(k\) 控制误差对奖励衰减的敏感程度。第二项 \(\lambda\|\omega_t-\omega_{t-1}\|_2\) 是权重变化惩罚，用来抑制权重频繁大幅跳变。 \(\lambda\) 越大，系统越强调融合权重平滑。

贝尔曼目标值为：

$$
y_t=r_t+\gamma\max_{a'}Q(S_{t+1},a';\theta^-)
$$

这里 \(y_t\) 是训练 DQN 时希望当前 Q 值逼近的目标。它由两部分组成：当前奖励 \(r_t\)，以及下一状态的最优未来价值。 \(\theta^-\) 表示目标网络参数，目标网络不是每一步都更新，这样可以减小训练目标的震荡。

损失函数为均方误差：

$$
L(\theta)=E_{(S_t,a_t,r_t,S_{t+1})\sim D}
\left[(y_t-Q(S_t,a_t;\theta))^2\right]
$$

这个损失衡量的是“网络当前预测的 Q 值”和“贝尔曼目标 Q 值”之间的差距。训练的目的不是直接最小化定位误差，而是通过奖励把定位误差和权重稳定性转化为 Q 值学习目标，再最小化 Q 值预测误差。经验回放池 \(D\) 用于打乱样本相关性，目标网络用于稳定训练。

## 第 17 页：PA-DQN 网络结构

这一页展示网络结构。输入是 6 维状态，分别来自物理特征、信号质量特征、一致性特征和历史权重特征。网络先把这些不同类型的输入嵌入到统一维度，形成 token 矩阵，然后经过共享前馈网络提取特征，最后由 DQN Head 输出动作价值向量。

这种结构的考虑是：物理感知状态中各个变量含义不同，量纲也不同，直接拼接可能难以学习。因此先做特征嵌入，再通过共享网络学习这些物理变量与动作价值之间的映射关系。

输出层对应动作集合的 Q 值。在答辩时可以强调：这里网络的作用是近似 \(Q(S,a)\)，输出的是各个候选调权动作的长期价值，不是直接输出定位坐标。按前面 9 类动作集合，输出维度应与动作数一致，即 9 个 Q 值。

## 第 18 页：单目标实验设置

这一页给出 PA-DQN 的训练设置和收敛情况。实验中采用复杂环境切换、多轨迹和低信噪比条件来验证方法的适应性。主要训练参数包括：动作步长 \(\Delta=0.05\)、经验回放容量 \(10^5\)、批量大小 64、折扣因子 \(\gamma=0.95\)、优化器 Adam、学习率 \(10^{-3}\)、目标网络每 200 步更新一次，\(\epsilon\)-greedy 从 1.0 衰减到 0.05。

左侧曲线显示训练过程中累计奖励上升并趋于稳定，说明智能体逐渐学习到有效的调权策略。损失曲线整体下降，说明网络预测 Q 值与贝尔曼目标之间的差距逐渐减小。这里实验验证的是：PA-DQN 不仅能够在训练中收敛，而且能够在复杂环境下形成稳定的融合决策。

## 第 19 页：矩形轨迹与对角线轨迹结果

这一页展示矩形轨迹和对角线轨迹下的定位效果。可以看到，单独使用 PDR 或 AOA 时轨迹容易出现漂移或偏离，EKF 能够一定程度平滑误差，但当观测源质量发生变化时仍然会受到影响。RL-IFA 相比固定融合有所提升，但由于状态表达较粗，遇到复杂环境切换时泛化能力有限。

PA-DQN 的结果整体更贴近真实轨迹，尤其在转弯、遮挡变化和轨迹方向变化处，能够通过物理状态调整融合权重，因此误差更小、轨迹更连续。

## 第 20 页：反对角线轨迹与随机游走结果

这一页进一步展示反对角线和随机游走场景。随机游走轨迹相比规则轨迹更难，因为运动方向变化更频繁，PDR 更容易积累误差，AOA 和 RSSI 也会受到局部环境影响。

从图中可以看出，PA-DQN 在随机轨迹中仍然保持较好的跟踪效果，说明它不是只适配某一种固定轨迹，而是能够根据当前信号质量和物理状态动态改变对不同定位源的信任程度。

## 第 21 页：单目标整体性能对比

这一页给出整体误差统计。整体场景下，PDR-only 的 RMSE 为 12.8347 米，AOA-only 的 RMSE 为 15.2086 米，说明单一观测源在复杂城市环境下误差较大。EKF 的 RMSE 降到 7.3364 米，RL-IFA 进一步降到 5.9002 米，而 PA-DQN 达到 2.1468 米。

P95 表示 95% 分位误差，也就是 95% 的定位误差不超过该值。它反映的是尾部误差和鲁棒性，数值越小越好。整体场景下 PA-DQN 的 P95 为 3.8724 米，明显低于其他方法，说明它不仅平均误差小，而且大误差出现概率也更低。

从线性轨迹和随机游走两个子场景也能看到类似趋势。线性轨迹中 PA-DQN 的 RMSE 最低；随机游走中虽然任务更难，但 PA-DQN 仍保持最优。这说明物理感知状态和深度强化学习调权机制能够有效提升复杂环境下的连续定位能力。

## 第 22 页：不同 SNR 下的鲁棒性

这一页考察信噪比变化对定位性能的影响。横轴是 SNR，纵轴分别为 RMSE 和 P95。随着 SNR 降低，所有方法的误差都会上升，这是因为观测噪声增强后 RSSI、AOA 等测量质量下降。

但是 PA-DQN 曲线整体最低，且随 SNR 变化的波动更小。这说明 PA-DQN 在低信噪比条件下能够识别观测质量下降，并相应调整融合权重，减少劣质观测对最终定位结果的影响。因此，它体现出比固定融合和传统滤波方法更好的噪声鲁棒性。

## 第 23 页：消融实验

这一页通过消融实验验证各个模块的作用。完整 PA-DQN 的 RMSE 为 2.15 米，权重变化指标为 0.04，说明它同时实现了较高定位精度和较平滑的权重调整。

权重变化指标定义为：

$$
R_\omega=\frac{1}{T-1}\sum_{t=2}^{T}\|\omega_t-\omega_{t-1}\|_2
$$

这个指标衡量相邻时刻融合权重的平均变化幅度，数值越小表示权重越稳定。

去掉物理感知状态后，RMSE 上升到 4.89 米，说明 RMS 时延扩展、RSSI 方差和几何一致性等物理特征对识别环境状态非常关键。去掉增量动作后，权重变化指标明显增大，说明直接或粗粒度调权会导致融合不稳定。去掉稳定性奖励后，定位误差变化不算最大，但权重变化明显上升，说明奖励中的平滑项主要作用是抑制调权震荡。

因此，这一页证明了三个设计点的必要性：物理感知状态提升环境识别能力，增量式动作保证调权过程可控，稳定性奖励保证融合结果平滑。

## 第 24 页：第三部分过渡页

接下来进入第二项工作：基于空间信号表征的多目标协同鲁棒定位方法。前一部分解决的是单目标连续定位中的自适应融合问题，而这一部分关注更复杂的多目标并发定位。

这里的核心问题是：当多个地面终端同时存在，并且多无人机观测节点可能随机掉点时，如何在观测残缺、空间采样变稀疏的条件下，仍然稳定判断目标所在区域。

## 第 25 页：多目标并发定位问题

这一页说明为什么多目标定位不能简单沿用单目标峰值搜索。多目标集合可以表示为：

$$
P=\{p_1,p_2,\cdots,p_K\},\quad p_k=[x_k,y_k]^T
$$

其中 \(K\) 是目标数量，且在实际场景中可能并不确定。无人机采样数据表示为：

$$
D=\{(s_i,r_i)\}_{i=1}^N,\quad s_i=[x_i,y_i]^T
$$

这里 \(s_i\) 是采样位置，\(r_i\) 是该位置观测到的信号响应。多目标情况下，观测信号可以表示为：

$$
r_i=\sum_{k=1}^{K}h_k(s_i;p_k)+n_i
$$

这个公式说明，一个采样点处的 RSSI 响应不是单一目标贡献，而是多个目标信号响应叠加再加噪声。复杂城市环境中还会出现多径鬼影和局部多峰，使得传统峰值检测容易把伪峰误判成目标。

因此，本论文把问题从“寻找单个峰值点”转化为“在 Radio Map 上进行区域级空间判别”。这样可以减少局部峰值和噪声对最终定位的影响。

## 第 26 页：Radio Map 信号成像思路

这一页展示 Radio Map 的构建流程。首先，无人机获得离散 RSSI 样本：

$$
D=\{(s_i,r_i)\}_{i=1}^{N}
$$

然后把目标区域划分为规则网格：

$$
G=\{g_{u,v}\mid u=1,\cdots,U;\ v=1,\cdots,V\}
$$

其中 \(g_{u,v}\) 是第 \(u,v\) 个网格单元。接着通过插值或平滑映射函数 \(F(\cdot)\) 把离散观测映射为连续空间信号强度：

$$
\hat R(u,v)=F(D,g_{u,v})
$$

所有网格上的响应组成 Radio Map：

$$
R=[\hat R(u,v)]_{U\times V}
$$

最后不再直接寻找最大峰值，而是识别目标响应区域：

$$
T_{region}=\{B_1,B_2,\cdots,B_{\hat K}\}
$$

其中 \(B_m\) 表示候选目标区域。这样做的优势是：Radio Map 保留了空间邻域结构和响应纹理，能够让后续模型利用区域形态判断目标，而不是被单个噪声峰值干扰。

## 第 27 页：OSM + MATLAB 工具链与传播场建模

这一页介绍数据和传播场的构建方式。实验不是简单使用理想平面传播模型，而是先从 OSM 获取真实城市地理数据，包括建筑轮廓、道路结构和高度信息，再导入 MATLAB Site Viewer 进行三维城市场景重建。

随后通过 Antenna Toolbox 配置收发天线参数和无人机平台位置，通过 RF Propagation Toolbox 进行射线追踪传播计算。射线追踪能够刻画直射、反射、绕射和非视距传播，从而生成更加接近真实城市环境的 RSSI 空间分布。

最终输出的是覆盖 500 米乘 500 米目标区域的高分辨率传播数据，共覆盖 27546 个网格点。这些数据用于构建 Radio Map，并为多目标区域判别提供物理一致的输入。

## 第 28 页：三维 Radio Map 示例

这一页展示不同场景下的三维 RSSI 分布。可以看到，真实传播场并不是一个平滑单峰形状，而是存在多个局部峰值、凹陷和不规则纹理。这些现象来自建筑遮挡、反射、多径叠加以及非视距衰减。

这也解释了为什么传统峰值搜索不够可靠：最高峰不一定对应真实目标，真实目标附近也可能因为遮挡出现响应减弱。Radio Map 的作用是把这些复杂空间信号结构显式表达出来，让模型在二维或三维空间纹理上进行区域级判别。

## 第 29 页：多无人机掉点与残缺观测

这一页引入多无人机节点掉点问题。在完整协同观测时，16 架 UAV 可以形成较密集的空间采样和较好的几何约束。但实际系统中，部分无人机可能由于链路中断、能量限制、遮挡或任务冲突而失效，导致随机节点掉点。

节点可用性表示为：

$$
\delta=[\delta_1,\delta_2,\cdots,\delta_{N_u}]^T,\quad \delta_i\in\{0,1\}
$$

其中 \(\delta_i=1\) 表示第 \(i\) 个无人机节点可用，\(\delta_i=0\) 表示该节点掉点。空间覆盖率定义为：

$$
\rho_{cov}=\frac{1}{N_u}\sum_{i=1}^{N_u}\delta_i
$$

它反映当前有效节点占全部节点的比例。可用节点集合为：

$$
V_t^{act}=\{u_i\mid u_i\text{ 在时刻 }t\text{ 可用}\}
$$

掉点比例定义为：

$$
\eta_t=1-\frac{N_t^{act}}{N}
$$

掉点带来的问题不是少几个测量值这么简单，而是空间采样密度下降、几何基线被削弱、Radio Map 结构失真。因此需要让模型在残缺观测下仍然尽量保持接近完整观测的判别能力。

## 第 30 页：教师-学生 GAT 网络架构

这一页给出教师-学生 GAT 架构。每个无人机节点的特征表示为：

$$
X_i=[x_i,y_i,z_i,R_i]
$$

其中 \(x_i,y_i,z_i\) 是无人机节点空间位置，\(R_i\) 是对应观测到的 RSSI 或 Radio Map 相关信号特征。教师模型使用完整节点图输入，学生模型使用掉点后的残缺图输入。

GAT 的核心是图注意力机制。两个节点之间的注意力打分为：

$$
e_{ij}=\operatorname{LeakyReLU}(a^T[Wh_i\|Wh_j])
$$

其中 \(h_i,h_j\) 是节点特征，\(W\) 是线性变换矩阵，\(a\) 是注意力参数，\(\|\) 表示拼接。然后进行归一化：

$$
\alpha_{ij}=
\frac{\exp(e_{ij})}{\sum_{k\in N(i)}\exp(e_{ik})}
$$

归一化后的 \(\alpha_{ij}\) 表示节点 \(j\) 对节点 \(i\) 的重要性。节点更新为：

$$
h_i'=\sigma\left(\sum_{j\in N(i)}\alpha_{ij}Wh_j\right)
$$

也就是说，GAT 会根据节点之间的空间和信号关系，自适应聚合邻居信息。图级读出采用全局平均池化：

$$
h_G=\frac{1}{|V|}\sum_{i\in V}h_i
$$

最后通过 MLP 输出 400 个网格类别的概率分布。教师模型学习完整观测下的空间判别知识，学生模型通过蒸馏学习在残缺拓扑下逼近教师的判断。

## 第 31 页：知识蒸馏训练机制

这一页进一步说明教师-学生网络的训练过程。教师模型输入完整图，输出完整观测条件下的 logits \(z^T\)；学生模型输入缺失图，输出残缺观测条件下的 logits \(z^S\)。

教师和学生都通过带温度系数的 Softmax 得到软标签：

$$
q_m=\frac{\exp(z_m/T_d)}{\sum_{j=1}^{M}\exp(z_j/T_d)}
$$

其中 \(T_d\) 是蒸馏温度。温度越高，概率分布越平滑，能够表达类别之间的相似关系。例如目标真实网格附近的相邻网格也可能获得较高概率，这比 one-hot 标签包含更多空间结构信息。

学生网络的交叉熵损失为：

$$
L_{ce}=-\sum_{m=1}^{M}y_m\log(\hat y_m^S)
$$

它约束学生预测正确网格类别。知识蒸馏损失为：

$$
L_{kd}=T_d^2\sum_{m=1}^{M}q_m^T\log\frac{q_m^T}{q_m^S}
$$

它约束学生的软概率分布接近教师分布。总损失为：

$$
L=\alpha L_{ce}+\beta L_{kd}
$$

其中 \(\alpha\) 控制真实标签监督强度，\(\beta\) 控制蒸馏监督强度。这样训练后，学生模型不仅学习“正确类别是什么”，还学习教师在完整观测下形成的全局空间判别知识。

## 第 32 页：多目标实验设置与复杂度

这一页给出多目标实验设置。目标区域大小为 500 米乘 500 米，划分为 \(20\times20\) 网格，因此共有 400 个网格类别。无人机数量为 16 架，掉点比例设置为 0%、12.5%、25%、37.5% 和 50%，用于模拟不同程度的观测残缺。

网络方面，GAT 层数为 3，隐藏维度为 64，注意力头数为 4，Dropout 为 0.2，图级读出采用 Global Average Pooling，分类头结构为 \(64\rightarrow128\rightarrow400\)。训练使用 Adam 优化器，初始学习率 \(10^{-3}\)，权重衰减 \(10^{-4}\)，批量大小 64，最大训练 800 轮，学习率每 200 轮衰减 0.5，早停 patience 为 50。

知识蒸馏温度系数 \(T_d=4\)，损失权重 \(\alpha=1.0,\beta=0.7\)，数据集划分为 70%、15%、15%。复杂度方面，蒸馏主要增加训练阶段的教师前向和蒸馏损失计算；在推理阶段只使用学生模型，因此不会增加部署时的推理复杂度。

## 第 33 页：多目标实验结果

这一页展示 25% 掉点条件下的定位性能。Teacher 使用完整观测，平均误差为 9.8 米，RMSE 为 12.6 米，90% 误差为 21.8 米，作为性能上界参考。Student-NoKD 在缺失观测下平均误差上升到 22.4 米，说明直接训练残缺图学生模型会明显退化。Interp-Loc 通过插值补全有一定改善，平均误差为 17.6 米，但仍不能充分恢复完整观测的信息。

Student-KD 的平均误差为 12.8 米，RMSE 为 16.0 米，90% 误差为 26.4 米，明显优于 Student-NoKD 和 Interp-Loc，并且接近 Teacher。这说明知识蒸馏能够有效把完整观测下的空间判别知识迁移给残缺观测模型。

下方表格进一步比较不同掉点比例。随着掉点比例从 0% 增加到 50%，Student-NoKD 平均误差从 13.6 米上升到 38.9 米，而 Student-KD 只从 11.2 米上升到 17.3 米。这个结果说明，蒸馏方法对节点缺失具有更强鲁棒性，能够显著缓解观测残缺导致的性能退化。

## 第 34 页：致谢

以上就是我的全部汇报。本文围绕无人机载平台下的卫星互联网终端定位问题，分别从单目标连续定位和多目标协同鲁棒定位两个层面展开研究。

第一项工作提出物理感知 PA-DQN 自适应融合方法，通过物理状态建模、增量调权动作和双重奖励机制，实现 RSSI、AOA、PDR 多源定位结果的动态融合。第二项工作提出 Radio Map 空间信号表征和教师-学生 GAT 蒸馏框架，在多目标和多无人机掉点条件下提升区域级定位鲁棒性。

我的汇报到此结束，恳请各位老师批评指正，谢谢各位老师。

