\chapter{低成本无人机定位系统模型与定位理论基础}
\label{ch:fundamentals}

\section{系统任务场景与总体架构}
\label{sec:2_1}

\subsection{非法低轨卫星终端排查任务描述}
\label{subsec:2_1_1}

随着天地一体化通信网络的持续发展，低轨卫星通信系统在广域覆盖、应急接入和远距离信息传输等方面展现出显著优势，并逐渐成为新一代信息网络体系中的重要组成部分。以 Starlink 为代表的低轨卫星星座快速部署，在提升全球宽带接入能力的同时，也使相关终端的非法使用、异常接入和违规辐射问题日益突出。对于无线电安全监管而言，如何在复杂环境下快速发现并定位地面非法低轨卫星终端，已经成为具有现实工程价值的重要课题。

与传统蜂窝网络中的常规定位任务相比，非法低轨卫星终端排查具有明显的特殊性。首先，该类目标通常不向监管系统主动提供同步信息、位置信息或协议辅助信息，监测端只能依赖目标终端辐射信号及其传播特征实现位置估计，因此从任务属性上属于典型的非合作式无源定位问题。其次，非法终端往往部署于高层建筑密集区域、街道走廊内部或复杂城市边缘地带，信号传播过程中普遍存在遮挡、反射、绕射与散射等现象，使得理想视距传播条件难以长期成立。再次，在实际排查场景中，目标终端可能表现为临时出现、短时工作、动态移动甚至多目标并发辐射等复杂形态，这对系统的机动响应能力、连续跟踪能力和抗虚警能力提出了更高要求。

从任务目标上看，本文所研究的非法低轨卫星终端排查并不局限于简单的信号检测，而是要求在复杂城市环境中实现对目标终端空间位置的有效估计。若将待定位目标在离散时刻 $k$ 的状态表示为
\begin{equation}
\mathbf{x}_{k}
=
\left[
x_k,\,
y_k,\,
\dot{x}_k,\,
\dot{y}_k
\right]^T,
\label{eq:2_1_target_state}
\end{equation}
其中，$x_k$ 与 $y_k$ 表示目标在二维平面内的位置坐标，$\dot{x}_k$ 与 $\dot{y}_k$ 表示其速度分量，则非法终端排查任务的本质可理解为：在目标状态未知、传播条件复杂且观测受扰动影响的条件下，依托外部观测手段对 $\mathbf{x}_k$ 进行估计，并进一步实现对目标空间分布与运动状态的判定。

为了对上述问题进行统一描述，可将无人机平台对目标终端的定位过程抽象为一般观测模型
\begin{equation}
\mathbf{z}_{k}
=
h(\mathbf{x}_{k})+\mathbf{v}_{k},
\label{eq:2_2_observation_model}
\end{equation}
其中，$\mathbf{z}_k$ 表示第 $k$ 时刻接收到的观测向量，$h(\cdot)$ 表示由目标状态到观测空间的非线性映射关系，$\mathbf{v}_k$ 表示观测噪声及环境扰动项。式~\eqref{eq:2_2_observation_model} 表明，定位系统的输出精度不仅取决于目标本身的位置状态，还与传播环境、观测方式以及噪声水平密切相关。对于复杂城市环境中的非法终端排查而言，映射函数 $h(\cdot)$ 往往不再满足理想自由空间条件下的简单形式，而是受到多径传播、非视距遮挡和局部信号衰落的共同影响。

\subsection{低成本无人机定位系统总体架构}
\label{subsec:2_1_2}

在明确非法低轨卫星终端排查任务之后，有必要进一步给出本文所研究的低成本无人机定位系统总体架构。需要说明的是，本文所强调的系统架构并非针对具体硬件器件配置展开，而是从任务流程和观测链路角度，对无人机执行定位任务时所涉及的场景要素、信息流向和功能模块进行统一描述。这样的处理方式有助于在不引入过多硬件细节的前提下，为后续传播建模、经典定位方法分析以及智能定位算法设计提供清晰的问题背景。


从数据流向上看，地面非法终端发射的辐射信号在复杂城市环境中传播后，被无人机平台在不同位置接收。随后，系统从接收信号中提取与定位相关的观测信息，例如接收信号强度、到达方向、时间特征、轨迹递推信息以及后续章节中涉及的高维指纹特征等。这些观测量或特征并不直接等同于最终位置，而是作为定位解算的输入，经由相应模型或学习算法进一步映射为目标位置估计结果。因而，从系统角度看，定位过程可以理解为从空间信号观测到目标位置参数的映射过程。

为了统一描述这一过程，可将无人机定位系统抽象为离散时间状态空间模型
\begin{equation}
\mathbf{x}_{k}
=
f(\mathbf{x}_{k-1},\mathbf{u}_{k-1})+\mathbf{w}_{k-1},
\label{eq:2_3_state_equation}
\end{equation}
\begin{equation}
\mathbf{z}_{k}
=
h(\mathbf{x}_{k})+\mathbf{v}_{k},
\label{eq:2_4_measurement_equation}
\end{equation}
其中，$\mathbf{x}_k$ 表示目标在第 $k$ 时刻的状态，$\mathbf{u}_{k-1}$ 表示上一时刻对系统状态演化有影响的输入量，$f(\cdot)$ 为状态转移函数，$\mathbf{w}_{k-1}$ 为过程噪声；$\mathbf{z}_k$ 表示第 $k$ 时刻无人机获得的观测向量，$h(\cdot)$ 为观测映射函数，$\mathbf{v}_k$ 为观测噪声。式~\eqref{eq:2_3_state_equation}--式~\eqref{eq:2_4_measurement_equation} 给出了后续章节讨论定位问题的统一数学框架：目标状态随时间演化，而观测则由目标状态经过复杂传播与测量过程映射得到。

在本文场景下，状态量 $\mathbf{x}_k$ 可以表示目标终端的位置或运动状态；观测量 $\mathbf{z}_k$ 则取决于无人机当前所处空间位置及其接收到的信号特征。由于无人机在巡检过程中不断改变空间位置，系统所获得的观测集合本身具有明显的空间采样属性。也就是说，定位系统并不是一次性获取完整区域信息，而是在飞行过程中逐步积累对目标区域的空间认知。这一点对后续章节具有直接影响：在第三章中，这种机动观测所形成的多源信息将用于构建面向单目标连续定位的自适应融合框架；在第四章中，这类空间采样结果将进一步被组织为 Radio Map，用于多目标并发定位中的信号成像与判别。

进一步地，为体现无人机平台在定位任务中的空间观测特征，图~\ref{fig:ch2_uav_localization_scenario} 给出了无人机执行巡检飞行并对地面非法终端开展机动定位的场景示意。该图强调了系统在复杂城市环境下的空间关系与探测几何，为本章后续关于传播机理和定位模型的展开提供了基础。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.82\textwidth]{figures/ch2/1.jpeg}
    \caption{无人机机动观测下的非法终端定位场景示意图}
    \label{fig:ch2_uav_localization_scenario}
\end{figure}

综上，本文所研究的低成本无人机定位系统，本质上是一个以无人机机动观测为核心、以复杂环境下信号感知为基础、以目标位置估计为输出的动态定位系统。该系统的关键特征在于：一方面，观测平台具有空间机动性，能够通过飞行过程主动构造观测视角；另一方面，定位过程高度依赖复杂环境中的传播特性与观测质量。因此，后续研究不能仅从理想几何关系出发讨论定位问题，而必须进一步结合复杂城市环境下的传播机理与观测误差来源进行分析。


\section{复杂城市环境下的低轨卫星传播机理}
\label{sec:2_2}

\subsection{多径效应、时延扩展与信号衰落机制}
\label{subsec:2_2_1}

在复杂城市环境下，低轨卫星终端相关信号从发射端传播至无人机接收端的过程中，通常不会仅沿单一直达路径传播，而是会受到建筑物立面、街道边缘、玻璃幕墙以及地表散射体的共同影响，形成由直达径、反射径、绕射径和散射径叠加构成的复合传播过程。对于非法低轨卫星终端排查任务而言，这种传播条件的复杂化将直接改变接收端所观测到的信号结构，使理想环境下基于简单几何关系建立的定位模型难以长期成立。因此，在建立后续定位方法之前，有必要先从传播层面对多径效应、时延扩展以及信号衰落机制进行统一分析。

从信号模型角度看，若接收端共接收到 $L$ 条有效传播路径，则复基带接收信号可表示为
\begin{equation}
r(t)
=
\sum_{l=1}^{L}
\alpha_l
\,s(t-\tau_l)\,
e^{-j2\pi f_c\tau_l}
+
n(t),
\label{eq:2_5_multipath_signal}
\end{equation}
其中，$s(t)$ 为发射信号，$\alpha_l$ 和 $\tau_l$ 分别表示第 $l$ 条路径的复衰落系数和传播时延，$f_c$ 为载波频率，$n(t)$ 为加性噪声。式~\eqref{eq:2_5_multipath_signal} 表明，接收端实际观测到的信号是多条传播路径在幅度、相位和时延上的叠加结果，而非理想自由空间条件下的单一路径传输结果。

在复杂城市环境中，当无人机与目标终端之间存在视距链路时，接收信号中往往同时包含直达径与若干条反射路径；而当建筑物遮挡导致直达路径被阻断时，系统只能接收到经墙面反射、边缘绕射或局部散射后到达的非视距分量。此时，接收端观测信号不仅信噪比下降，而且其主导路径的几何意义也会发生偏移。例如，在方向估计问题中，最强到达路径的方向可能不再指向真实目标；在功率测距问题中，接收功率也会偏离理想路径损耗模型所对应的单调衰减关系。由此可见，多径效应带来的影响并不局限于噪声增强，而是会从根本上改变观测量与真实位置之间的映射关系。

为了更紧凑地描述多径传播结构，通常可引入信道冲激响应表示。对于时变城市传播环境，等效信道可写为
\begin{equation}
h(t,\tau)
=
\sum_{l=1}^{L}
\alpha_l(t)
e^{-j\phi_l(t)}
\delta\big(\tau-\tau_l(t)\big),
\label{eq:2_6_time_varying_cir}
\end{equation}
其中，$\alpha_l(t)$、$\phi_l(t)$ 和 $\tau_l(t)$ 分别表示第 $l$ 条路径在时刻 $t$ 的幅度、相位和传播时延。式~\eqref{eq:2_6_time_varying_cir} 说明，复杂城市环境中的信道本质上是由若干条随时间变化的多径分量共同构成的。对于无人机平台而言，由于平台自身处于运动状态，接收端的相对观测几何会随航迹不断变化，因此多径结构不仅受环境决定，也会随无人机位置变化而动态演化。

在多径环境下，不同路径具有不同到达时延，从而导致接收信号能量在时域上被拉宽。为了度量这一离散程度，通常定义平均时延和均方根时延扩展。设第 $l$ 条路径的平均功率为 $P_l$，则平均时延可表示为
\begin{equation}
\bar{\tau}
=
\frac{\sum_{l} P_l \tau_l}{\sum_{l} P_l},
\label{eq:2_7_mean_delay}
\end{equation}
对应的均方根时延扩展为
\begin{equation}
\sigma_{\tau}
=
\sqrt{
\frac{\sum_{l} P_l(\tau_l-\bar{\tau})^2}{\sum_{l} P_l}
},
\label{eq:2_8_rms_delay_spread}
\end{equation}
其中，$\sigma_{\tau}$ 越大，表明信号能量在时域上的分散程度越高，接收端越难稳定区分主导路径与附加路径。对于定位任务而言，时延扩展的增大通常意味着传播环境更复杂，路径重叠更严重，观测结果更容易受到多径干扰。

需要指出的是，均方根时延扩展不仅是传统无线传播分析中的重要参数，在本文后续方法中也具有直接作用。在第三章中，$\sigma_{\tau}$ 将被作为表征传播复杂度的重要物理感知特征，用于强化学习智能体的状态构造；在第四章中，由多径引起的时域离散和空间叠加效应，又会进一步表现为空间热力图中的局部高亮扩散、纹理模糊和伪热点增强。因此，从论文整体逻辑来看，时延扩展不仅是一个信道统计量，也是连接传播机理与后续智能定位方法的重要桥梁。

除时域离散外，多径传播还会引起显著的功率衰落与信号起伏。若仅从大尺度传播角度考察，接收功率常采用对数距离模型描述：
\begin{equation}
P_r(d)
=
P_t
-
PL(d_0)
-
10n\log_{10}\frac{d}{d_0}
+
X_{\sigma},
\label{eq:2_9_log_distance_model}
\end{equation}
其中，$P_t$ 为发射功率，$PL(d_0)$ 为参考距离 $d_0$ 处的路径损耗，$n$ 为路径损耗指数，$X_{\sigma}$ 为阴影衰落项。式~\eqref{eq:2_9_log_distance_model} 表明，即使在相同几何距离条件下，接收功率也会因环境遮挡、建筑分布和局部传播条件差异而出现明显波动。在城市峡谷环境中，这种波动通常表现得更为剧烈，从而使 RSSI 与传播距离之间的对应关系明显弱化。


\subsection{Starlink Ku 波段下行链路帧结构}
\label{subsec:2_2_2}

现有公开研究表明，Starlink 在 Ku 波段下行链路中采用正交频分复用（OFDM）体制，其信号在频域上表现为边界清晰、带内功率近似平坦的多子载波结构。对于第 $m$ 帧中第 $i$ 个 OFDM 符号，其基带时域信号可写为
\begin{equation}
x^{(0)}_{m,i}(t)
=
\sum_{k=0}^{N-1}
X_{m,i,k}
\exp(j2\pi Fkt),
\qquad 0\le t<T,
\label{eq:2_10_starlink_ofdm_symbol}
\end{equation}
其中，$X_{m,i,k}$ 表示第 $k$ 个子载波上的复值调制符号，$N$ 为子载波数，$F$ 为子载波间隔，$T$ 为有效符号时长。对于 OFDM 系统，有
\begin{equation}
T=\frac{N}{F_s},
\qquad
F=\frac{F_s}{N},
\label{eq:2_11_starlink_ofdm_basic}
\end{equation}
其中，$F_s$ 表示信道带宽或等效采样率。

为抑制多径条件下的符号间干扰，Starlink OFDM 符号前还包含循环前缀。若循环前缀长度对应的时间为 $T_g$，则加入循环前缀后的 OFDM 符号时长为
\begin{equation}
T_{\mathrm{sym}}=T+T_g.
\label{eq:2_12_starlink_symbol_duration}
\end{equation}

\section{经典定位方法模型}
\label{sec:2_3}

在前文中，本文已经对非法低轨卫星终端排查任务场景、系统总体架构以及复杂城市环境下的传播机理进行了分析。可以看到，信号在从地面发送至无人机的过程中，会受到多径叠加、阴影衰落和非视距遮挡的共同影响，这些因素直接决定了接收端所获取观测量的质量。为了建立后续智能定位算法的基础，本节首先对 RSSI、AoA、TOA、TDOA 和 PDR 等经典定位方法的基本模型、几何约束以及在复杂城市环境中的局限性进行统一总结。

\subsection{RSSI 定位}
\label{subsec:2_3_1}

接收信号强度指示（Received Signal Strength Indicator, RSSI）定位是一类基于信号功率衰减特性的经典定位方法，其基本思想是利用接收端测得的信号强度与传播距离之间的统计关系，反推出目标与观测平台之间的相对距离，再结合多个观测位置的几何关系实现目标位置估计。由于 RSSI 可直接由接收信号获取，对额外同步机制和复杂阵列结构的依赖较弱，因此该方法在低成本定位场景中具有实现简单、部署方便和计算开销较低等优点。

在理想自由空间条件下，信号功率随传播距离增大而逐渐衰减，其基本规律可由路径损耗模型描述。若采用对数距离路径损耗模型，则距离 $d$ 处的接收功率可写为
\begin{equation}
P_r(d)
=
P_t
-
PL(d_0)
-
10n\log_{10}\frac{d}{d_0}
+
X_{\sigma},
\label{eq:2_15_rssi_pathloss_model}
\end{equation}
其中，$P_t$ 为发射功率，$PL(d_0)$ 为参考距离 $d_0$ 处的路径损耗，$n$ 为路径损耗指数，$X_{\sigma}$ 为阴影衰落项。若暂不考虑随机扰动项，则可以根据接收功率反推出目标到观测点之间的距离估计值
\begin{equation}
\hat d
=
d_0
\cdot
10^{\frac{P_t-PL(d_0)-P_r(d)}{10n}}.
\label{eq:2_16_rssi_distance_estimation}
\end{equation}

式~\eqref{eq:2_16_rssi_distance_estimation} 给出了 RSSI 测距的基本形式。对于单个观测点而言，$\hat d$ 对应的是一个以观测点为圆心、以估计距离为半径的等距圆；当无人机平台在多个空间位置获得 RSSI 观测时，即可形成多个距离约束，并通过几何交会估计目标位置。若第 $i$ 个观测位置坐标为 $(x_i,y_i)$，目标位置为 $(x,y)$，则对应的几何关系可写为
\begin{equation}
\sqrt{(x-x_i)^2+(y-y_i)^2}
=
\hat d_i,
\qquad i=1,2,\dots,M,
\label{eq:2_17_rssi_geometry_constraint}
\end{equation}
其中，$M$ 为有效观测位置数，$\hat d_i$ 为第 $i$ 个观测位置对应的 RSSI 测距结果。当 $M\geq 3$ 时，理论上可通过最小二乘等方式求解目标位置。


\subsection{AoA 定位}
\label{subsec:2_3_2}

到达角（Angle of Arrival, AoA）定位是一类基于信号空间传播方向的经典定位方法。其基本原理是利用具有方向性感知能力的接收天线阵列，通过测量入射信号在阵列上的相位差或功率分布，估计信号相对于观测平台的到达方位角，并根据测量出的方位线与多个观测位置的几何关系实现对目标的定位。

设目标在地面二维平面上的坐标为 $\mathbf{x} = [x, y]^T$，第 $i$ 次观测时无人机的空间位置坐标为 $(x_i, y_i)$，则其观测到的目标方位角 $\theta_i$ 与目标位置之间的几何关系为
\begin{equation}
\theta_i
=
\arctan\left( \frac{y - y_i}{x - x_i} \right)
+
\epsilon_{\theta,i},
\label{eq:2_18_aoa_observation_model}
\end{equation}
其中，$\epsilon_{\theta,i}$ 为角度测量噪声。式~\eqref{eq:2_18_aoa_observation_model} 表明，一次 AoA 观测可以在空间中确定一条以观测点为起点、指向目标方向的方位射线。当无人机在多个不同位置开展机动观测时，可以形成多条方位射线的交会，从而确定目标位置。

图~\ref{fig:ch2_aoa_localization} 展示了 AoA 定位的几何原理。该图突出了无人机在不同航迹点获取方位射线，并由射线交点反推目标位置的过程。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.78\textwidth]{figures/ch2/2.png}
    \caption{AoA 定位几何约束示意图}
    \label{fig:ch2_aoa_localization}
\end{figure}

\subsection{TOA 定位}
\label{subsec:2_3_3}

TOA（Time of Arrival）定位方法通过测量信号从发射端传播到接收端的绝对到达时间实现对传播距离的估计。设第 $i$ 个接收机测得传播时延为 $t_i$，则距离满足 $\hat{d}_i=ct_i$。其几何约束为：
\begin{equation}
\sqrt{(x-x_i)^2+(y-y_i)^2}=ct_i,
\label{eq:2_19_toa_circle}
\end{equation}
这意味着每个 TOA 观测对应一条以接收机为圆心的等距圆。图~\ref{fig:ch2_toa_localization} 展示了 TOA 定位的测距几何关系。TOA 在理想条件下具有清晰的距离物理意义，且理论定位精度较高，但其前提是系统能够获得高精度绝对时间同步。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.78\textwidth]{figures/ch2/3.png}
    \caption{TOA 定位几何约束示意图}
    \label{fig:ch2_toa_localization}
\end{figure}

\subsection{TDOA 定位}
\label{subsec:2_3_4}

TDOA（Time Difference of Arrival）定位方法通过不同接收机之间的到达时间差消除未知发射时刻的影响。设目标到第 $i$ 个接收机和参考接收机 1 的传播距离分别为 $d_i$ 和 $d_1$，则有：
\begin{equation}
\sqrt{(x-x_i)^2+(y-y_i)^2}-\sqrt{(x-x_1)^2+(y-y_1)^2}=c\Delta t_{i,1},
\label{eq:tdoa_hyperbola}
\end{equation}
式~\eqref{eq:tdoa_hyperbola} 对应一条双曲线，因此 TDOA 的定位本质是利用多条双曲线的交会确定目标位置。图~\ref{fig:ch2_tdoa_localization} 描绘了基于双曲线交会的 TDOA 定位原理。相较于 TOA，TDOA 不需要知道目标的绝对发射时刻，因此在非合作定位中具有更强的工程实用性。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.78\textwidth]{figures/ch2/4.png}
    \caption{TDOA 定位几何约束示意图}
    \label{fig:ch2_tdoa_localization}
\end{figure}

\subsection{PDR 定位}
\label{subsec:2_3_5}

位置轨道预测（Position Dead Reckoning, PDR）是一种利用先验运动学模型，通过积分或离散累加前一时刻的位置来推算当前位置的手段。在离散时间二维空间中，PDR 定位模型的标准状态更新公式可表示为：
\begin{equation}
\begin{bmatrix} x_k \\ y_k \end{bmatrix} = \begin{bmatrix} x_{k-1} \\ y_{k-1} \end{bmatrix} + \Delta d_k \begin{bmatrix} \cos \theta_k \\ \sin \theta_k \end{bmatrix} + \mathbf{w}_k,
\label{eq:2_21_pdr_update}
\end{equation}
其中，$(x_k, y_k)^T$ 表示目标在 $k$ 时刻的二维坐标，$\Delta d_k$ 为步长，$\theta_k$ 为运动航向角，$\mathbf{w}_k$ 为过程噪声。图~\ref{fig:ch2_pdr_localization} 给出了 PDR 运动递推的基本逻辑示意。PDR 方法的优势在于其完全自主，不受城市峡谷中多径和信号衰落的影响，但其缺陷在于误差会随时间累积产生偏移（Drift Error）。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.78\textwidth]{figures/ch2/5.png}
    \caption{PDR 定位运动递推示意图}
    \label{fig:ch2_pdr_localization}
\end{figure}
\section{智能定位方法基础}
\label{sec:2_4}

在前文中，本文已经对复杂城市环境下的传播机理以及 RSSI、AoA、TOA、TDOA 和 PDR 等经典定位方法进行了分析。可以看到，在低成本无人机平台与复杂传播环境的共同约束下，单一经典定位观测量往往难以同时兼顾精度、鲁棒性与实时性。一方面，复杂环境会使观测模型发生明显失配；另一方面，目标状态、观测质量和环境结构又随时间不断变化，使静态规则或固定模型难以长期保持最优性能。正因如此，后续研究需要进一步引入能够适应复杂状态变化的数据驱动方法与智能学习机制。

从全文方法安排看，后续章节需要引入能够适应复杂状态变化的数据驱动方法与智能学习机制。本节主要聚焦于与本文后续方法直接相关的理论基础，即马尔可夫决策过程、DQN 强化学习基础以及知识蒸馏基础。

\subsection{马尔可夫决策过程}
\label{subsec:2_4_1}

在动态环境中，定位系统并不是在单一时刻独立完成一次位置估计，而是需要随着目标状态、观测质量和环境条件的变化不断进行决策更新。特别是在第三章所研究的多源异构信息融合定位问题中，系统需要根据当前观测特征动态调整不同定位分支的融合权重，而这种权重调整不仅影响当前时刻的定位结果，也会进一步影响后续时刻的轨迹连续性与误差传播。因此，从问题本质上看，该类定位过程具有明显的序列决策特征。

马尔可夫决策过程（Markov Decision Process, MDP）为这类问题提供了统一建模框架。一般而言，一个 MDP 可表示为五元组
\begin{equation}
\mathcal{M}
=
\langle
\mathcal{S},
\mathcal{A},
\mathcal{P},
\mathcal{R},
\gamma
\rangle,
\label{eq:2_25_mdp_tuple}
\end{equation}
其中，$\mathcal{S}$ 表示状态空间，$\mathcal{A}$ 表示动作空间，$\mathcal{P}$ 表示状态转移概率，$\mathcal{R}$ 表示奖励函数，$\gamma\in[0,1]$ 为折扣因子。对于任意时刻 $t$，智能体观测当前状态 $s_t\in\mathcal{S}$，选择动作 $a_t\in\mathcal{A}$，动作执行后系统获得即时奖励 $r_t$，并转移到下一状态 $s_{t+1}$。

从本文定位问题出发，状态可以理解为当前环境、观测质量和历史决策信息的综合描述；动作可以理解为对定位策略、融合权重或网络输出的调节操作；奖励则用于衡量当前决策在定位精度、稳定性或鲁棒性上的优劣。若系统未来状态主要由当前状态 and 当前动作决定，而与更早之前的历史无直接关系，则可近似视为满足马尔可夫性质，即
\begin{equation}
P(s_{t+1}\mid s_t,a_t,s_{t-1},a_{t-1},\dots)
=
P(s_{t+1}\mid s_t,a_t).
\label{eq:2_26_markov_property}
\end{equation}

在 MDP 框架下，智能体的目标并不是追求单步奖励最大，而是寻求长期累计回报最大。设策略 $\pi$ 表示状态到动作的映射，则对应的状态—动作价值函数可写为
\begin{equation}
Q^{\pi}(s,a)
=
\mathbb{E}_{\pi}
\left[
\sum_{k=0}^{\infty}\gamma^k r_{t+k}
\;\middle|\;
s_t=s,\; a_t=a
\right].
\label{eq:2_27_q_function}
\end{equation}
式~\eqref{eq:2_27_q_function} 表明，一个动作的优劣不仅取决于其带来的即时收益，还取决于它对未来状态演化和长期定位效果的影响。这一点与复杂环境下的连续定位过程高度一致，因为某一时刻的错误决策往往会进一步放大后续误差。

进一步地，依据 Bellman 最优性原理，最优状态—动作价值函数满足
\begin{equation}
Q^*(s,a)
=
\mathbb{E}
\left[
r_t+\gamma \max_{a'}Q^*(s',a')
\;\middle|\;
s,a
\right],
\label{eq:2_28_bellman_optimality}
\end{equation}
其中，$s'$ 表示执行动作 $a$ 后系统到达的下一状态。该式为后续强化学习求解提供了理论基础。

图~\ref{fig:ch2_mdp_framework} 给出了本文定位问题中的 MDP 建模示意图，清晰展现了状态观测、动作执行与奖励反馈之间的闭环交互过程。该框架为第三章中基于物理感知的多源异构信息自适应融合提供了核心方法学支撑。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.80\textwidth]{figures/ch2/6.png}
    \caption{定位问题中的马尔可夫决策过程建模示意图}
    \label{fig:ch2_mdp_framework}
\end{figure}

综上，马尔可夫决策过程为复杂环境下的动态定位问题提供了统一描述方式。通过将状态感知、策略选择和奖励反馈纳入同一闭环框架，MDP 为后续第三章中基于强化学习的多源异构融合定位方法奠定了理论基础。

\subsection{DQN 强化学习基础}
\label{subsec:2_4_2}

在马尔可夫决策过程框架下，若状态空间和动作空间均较小，则可采用表格式方法直接存储和更新状态—动作价值函数。然而，对于复杂环境下的定位问题而言，系统状态通常具有连续性和高维特征，若仍采用表格式方法，不仅难以有效覆盖状态空间，还会导致维数灾难问题。特别是在第三章中，定位状态由 RMS 时延扩展、RSSI 方差、几何一致性特征以及历史权重等信息共同构成，其状态空间显然不再适合直接使用传统 Q 表存储。基于这一现实需求，深度 Q 网络（Deep Q-Network, DQN）被引入用于逼近状态—动作价值函数。

DQN 的核心思想，是利用参数为 $\theta$ 的神经网络近似最优状态—动作价值函数，即
\begin{equation}
Q(s,a;\theta)\approx Q^*(s,a).
\label{eq:2_29_dqn_approx}
\end{equation}
对于给定状态 $s_t$，网络输出所有可选动作对应的 Q 值估计，智能体再从中选择最优动作。由此，传统 Q-learning 中依赖显式 Q 表的更新方式，被替换为对神经网络参数的迭代优化。

依据 Bellman 最优性原理，DQN 中的目标值可定义为
\begin{equation}
y_t
=
r_t+\gamma \max_{a'}Q(s_{t+1},a';\theta^-),
\label{eq:2_30_dqn_target}
\end{equation}
其中，$\theta^-$ 表示目标网络参数。进而，当前网络的损失函数可写为
\begin{equation}
L(\theta)
=
\mathbb{E}_{(s_t,a_t,r_t,s_{t+1})\sim\mathcal{D}}
\left[
\left(
y_t-Q(s_t,a_t;\theta)
\right)^2
\right],
\label{eq:2_31_dqn_loss}
\end{equation}
其中，$\mathcal{D}$ 表示经验回放池中的样本分布。通过最小化式~\eqref{eq:2_31_dqn_loss}，即可不断调整网络参数，使其逼近最优 Q 函数。

在实际训练过程中，DQN 通常包含两个关键稳定机制。其一是经验回放（Experience Replay），即将智能体在交互过程中获得的样本 $(s_t,a_t,r_t,s_{t+1})$ 存入经验池中，再从中随机采样构成训练批次，从而打破时间序列样本之间的强相关性。其二是目标网络（Target Network），即使用参数更新较慢的网络 $\theta^-$ 生成目标值 $y_t$，以避免训练目标随当前网络快速波动而导致不稳定。正是这两个机制，使 DQN 能够在复杂状态空间中保持相对稳定的学习过程。

从定位应用角度看，DQN 相较于传统表格式 Q-learning 的优势主要体现在两个方面。首先，它能够处理连续或高维状态输入，从而适应复杂环境下由多种物理特征与历史信息共同构成的状态描述；其次，神经网络的非线性映射能力使其能够从复杂状态中自动学习对动作价值的近似表达，而无需人工对状态进行过度离散化。这一点对于本文第三章中的物理感知融合定位尤为关键。

综上，DQN 在保留强化学习长期决策优势的基础上，利用深度神经网络实现了对高维状态空间中价值函数的有效逼近。对于本文第三章所研究的动态融合定位问题而言，DQN 提供了将复杂环境感知状态直接映射到融合决策动作的可行工具。

\subsection{知识蒸馏基础}
\label{subsec:2_4_3}

除面向序列决策的强化学习方法外，本文第四章还将涉及另一类重要的智能学习机制，即知识蒸馏（Knowledge Distillation）。其基本思想是：利用性能更强、输入信息更完整或训练更充分的教师模型，将其学习到的判别知识迁移给学生模型，使后者在输入受限、结构受限或观测残缺的条件下仍能够逼近教师模型的性能。对于本文后续研究而言，知识蒸馏尤其适用于多无人机协同定位中存在随机掉点和节点失效的场景。

从一般意义上看，知识蒸馏建立在教师—学生框架之上. 设教师网络输出 logits 向量为 $\mathbf{z}^{T}$，学生网络输出 logits 向量为 $\mathbf{z}^{S}$，则在温度系数 $T_d$ 作用下，教师网络的软标签分布可表示为
\begin{equation}
q^{T}_{m}
=
\frac{\exp(z^{T}_{m}/T_d)}
{\sum_{n}\exp(z^{T}_{n}/T_d)},
\label{eq:2_32_teacher_soft}
\end{equation}
学生网络对应的软化输出为
\begin{equation}
q^{S}_{m}
=
\frac{\exp(z^{S}_{m}/T_d)}
{\sum_{n}\exp(z^{S}_{n}/T_d)}.
\label{eq:2_33_student_soft}
\end{equation}
其中，$m$ 表示第 $m$ 个类别或空间栅格索引。相比于 one-hot 硬标签，软标签分布包含了类别间更细粒度的相对相似性信息，因此能够为学生网络提供更丰富的监督信号。

图~\ref{fig:ch2_distillation_framework} 展示了知识蒸馏中典型的教师—学生学习架构。通过这种机制，学生网络能够在缺失部分节点观测的极端情况下，通过学习教师模型的深层判别特征来维持定位性能的稳健性。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.82\textwidth]{figures/ch2/7.png}
    \caption{知识蒸馏教师—学生学习机制示意图}
    \label{fig:ch2_distillation_framework}
\end{figure}

综上，知识蒸馏通过引入教师模型的软标签监督，为输入受限或观测不完整条件下的学生模型提供了更强的判别引导能力。这一机制为本文第四章中面向多无人机掉点场景的鲁棒定位方法提供了直接理论基础。

\section{定位性能评估}
\label{sec:2_5}

在完成经典定位模型与智能定位方法基础介绍之后，还需要进一步给出本文后续实验分析中采用的性能评估方式。对于定位问题而言，方法优劣最终需要通过定量指标进行衡量，而这些指标既应反映整体精度，也应能够刻画极端误差和理论性能上限。基于这一考虑，本文从定位误差评价指标和克拉美罗下界两个角度，对定位性能评估基础进行说明。

\subsection{定位误差评价指标}
\label{subsec:2_5_1}

定位误差是衡量定位系统性能最直接的指标。设目标真实位置为
\begin{equation}
\mathbf{p}^{\mathrm{gt}}
=
[x^{\mathrm{gt}},y^{\mathrm{gt}}]^T,
\label{eq:2_36_gt_position}
\end{equation}
定位算法输出的位置估计为
\begin{equation}
\hat{\mathbf{p}}
=
[\hat{x},\hat{y}]^T,
\label{eq:2_37_est_position}
\end{equation}
则单次定位误差可定义为二者之间的欧氏距离
\begin{equation}
e
=
\left\|
\hat{\mathbf{p}}-\mathbf{p}^{\mathrm{gt}}
\right\|_2
=
\sqrt{
(\hat{x}-x^{\mathrm{gt}})^2
+
(\hat{y}-y^{\mathrm{gt}})^2
}.
\label{eq:2_38_single_error}
\end{equation}

若共有 $N$ 个测试样本，则平均定位误差（Mean Error）可表示为
\begin{equation}
\mathrm{Mean\ Error}
=
\frac{1}{N}\sum_{i=1}^{N} e_i,
\label{eq:2_39_mean_error}
\end{equation}
均方根误差（RMSE）可表示为
\begin{equation}
\mathrm{RMSE}
=
\sqrt{
\frac{1}{N}\sum_{i=1}^{N} e_i^2
},
\label{eq:2_40_rmse}
\end{equation}
其中，$e_i$ 表示第 $i$ 个样本的定位误差。Mean Error 用于反映整体平均偏差水平，RMSE 则对较大误差更为敏感，因此更适合作为总体精度的综合衡量指标。

除均值类指标外，尾部误差指标同样具有重要意义。对于连续定位或复杂环境定位任务而言，偶发性的大误差往往会直接导致轨迹推算跳变、目标误判或系统失稳。因此，本文后续还将使用百分位误差，如 $90\%$ 分位误差或 $95\%$ 分位误差，对误差分布尾部进行刻画。若记误差样本按从小到大排序后的 $p\%$ 分位值为 $e_{p}$，则其含义为：有 $p\%$ 的测试样本定位误差不超过该值。与均值指标相比，百分位误差更能反映系统在极端场景下的稳定性与工程可用性。

此外，在第四章多目标定位实验中，还将进一步采用误差累计分布函数（CDF）对整体误差分布进行描述。CDF 曲线的横轴表示误差阈值，纵轴表示误差不超过该阈值的样本累计比例。该图形化指标能够较直观地比较不同方法在整体误差分布上的优劣，尤其适合展示蒸馏增强模型与未增强模型之间的差异。

综上，本文后续实验中采用的定位误差评价指标将主要包括 Mean Error、RMSE、百分位误差以及误差 CDF 曲线等形式。它们分别对应整体平均性能、较大误差敏感性、尾部鲁棒性和误差分布形态，从不同角度共同支撑定位性能分析。

\subsection{克拉美罗下界分析基础}
\label{subsec:2_5_2}

除实验统计指标外，克拉美罗下界（Cramér--Rao Lower Bound, CRLB）也是分析定位系统理论性能的重要工具。其基本作用在于：在给定观测模型与噪声统计条件下，给出无偏估计器协方差所能达到的理论下界，从而反映系统在理想条件下的最优可达性能。

对于一般定位问题，可将观测模型统一写为
\begin{equation}
\mathbf{z}
=
\mathbf{h}(\mathbf{p})
+
\mathbf{n},
\label{eq:2_41_general_obs}
\end{equation}
其中，$\mathbf{p}=[x,y]^T$ 为待估位置参数，$\mathbf{z}$ 为观测向量，$\mathbf{h}(\mathbf{p})$ 为位置到观测空间的映射函数，$\mathbf{n}$ 为观测噪声。若噪声协方差矩阵为 $\mathbf{R}$，则 Fisher 信息矩阵可表示为
\begin{equation}
\mathbf{J}(\mathbf{p})
=
\left(
\frac{\partial \mathbf{h}}{\partial \mathbf{p}}
\right)^T
\mathbf{R}^{-1}
\left(
\frac{\partial \mathbf{h}}{\partial \mathbf{p}}
\right),
\label{eq:2_42_fim}
\end{equation}
其中，$\frac{\partial \mathbf{h}}{\partial \mathbf{p}}$ 表示观测函数对位置参数的雅可比矩阵。

在无偏估计条件下，位置估计协方差满足
\begin{equation}
\mathrm{Cov}(\hat{\mathbf{p}})
\succeq
\mathbf{J}^{-1}(\mathbf{p}),
\label{eq:2_43_crlb_cov}
\end{equation}
即任何无偏估计器的误差协方差都不可能优于 Fisher 信息矩阵逆所给出的下界。进一步地，对于二维定位问题，其均方根误差下界可写为
\begin{equation}
\mathrm{RMSE}
\ge
\sqrt{
\mathrm{tr}
\left(
\mathbf{J}^{-1}(\mathbf{p})
\right)
}.
\label{eq:2_44_crlb_rmse}
\end{equation}

CRLB 的意义在于，它不依赖于具体求解算法，而仅由观测模型 and 噪声统计决定。因此，可利用 CRLB 对不同观测量组合在理想条件下的理论可达精度进行比较。例如，对于 RSSI、AoA、TOA 和 TDOA 等经典方法，若其观测模型 and 噪声特性已知，则均可通过式~\eqref{eq:2_42_fim}--式~\eqref{eq:2_44_crlb_rmse} 计算理论误差下界。

需要指出的是，复杂城市环境中的实际定位误差通常会高于理想 CRLB。其原因在于，CRLB 假设观测模型准确且噪声统计可知，而现实环境中广泛存在多径传播、非视距偏差、模型失配和观测异常等问题，这些因素都会使实际误差偏离理想下界。然而，即便如此，CRLB 仍然是分析定位理论性能上限的重要参照。对于本文而言，它的价值更多体现在理论层面：一方面说明不同观测模型的潜在精度差异，另一方面为后续实验结果分析提供理想性能对照。

综上，定位误差评价指标与克拉美罗下界共同构成了本文后续章节的性能分析基础。前者用于从实验层面定量评估定位方法的实际效果，后者则用于从理论层面衡量观测模型的可达性能边界。二者结合后，可较完整地支撑后续第三章与第四章中的实验结果分析与方法比较。

\section{本章小结}
\label{sec:2_6}

本章围绕低成本无人机平台下的非法低轨卫星终端定位问题，系统梳理了后续研究所需的系统模型与理论基础。首先，结合非法终端排查任务特点，给出了低成本无人机机动观测场景下的系统任务描述与总体架构，明确了本文所研究问题属于复杂城市环境中的非合作式无源定位问题。其次，从传播层面对复杂城市环境中的多径效应、时延扩展和信号衰落机制进行了分析，并以 Starlink Ku 波段下行链路为典型对象，对后续信号建模与特征提取所依赖的体制背景进行了说明。

在此基础上，本章进一步总结了 RSSI、AoA、TOA、TDOA 和 PDR 等经典定位方法模型，分析了不同观测量所对应的几何约束关系及其在复杂城市环境中的适用边界。相关分析表明，在低成本无人机平台与复杂传播环境的双重约束下，单一经典定位观测量往往难以同时兼顾高精度、强鲁棒和低复杂度，这为后续引入多源融合、强化学习与知识蒸馏等方法提供了问题背景。

最后，本章从智能定位方法基础与性能评估基础两个方面，为后续章节展开做了铺垫。马尔可夫决策过程与 DQN 强化学习基础，为第三章中基于物理感知深度强化学习的单目标自适应融合定位方法提供了理论支撑；知识蒸馏基础，则为第四章中面向多无人机掉点场景的鲁棒定位方法奠定了理论基础。同时，Mean Error、RMSE、百分位误差以及克拉美罗下界等性能评估指标，也为后续实验分析提供了统一评价依据。

综上，本章完成了从任务场景、传播机理、经典定位模型到智能方法基础的整体铺垫，为后续第三章单目标连续定位方法和第四章多目标并发定位方法的研究奠定了统一的理论基础。
