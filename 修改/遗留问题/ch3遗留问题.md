# ch3 遗留问题

本文档记录第三章当前仍未处理、后续需要继续思考或补写的问题。以下内容暂未直接修改正文。

## 1. `PDR` 信息源来源解释不足

- 当前第三章把 `PDR` 作为核心融合分支之一，但章节开头同时将场景定义为“非合作式无源地面非法终端定位”。
- 现有文本没有解释系统如何获取目标侧 `PDR` 信息。
- 后续需要明确：
  - `PDR` 是否确实来自目标侧惯导信息；
  - 如果不是，是否应改写为“运动先验分支”“轨迹递推分支”或“状态预测分支”；
  - 如果有特殊前提假设，需要在第三章前部明确写出。

## 2. 训练阶段与推理阶段的边界说明仍不够统一

- 当前实验设置已经写了“离线训练、在线推理”，但前文仍存在“闭环在线更新”等措辞。
- 后续建议统一说明：
  - 训练阶段依赖仿真真值构造奖励并更新策略；
  - 推理阶段仅执行前向决策，不做参数在线更新。

## 3. 奖励函数依赖真值的前提说明仍需补清

- `RL-IFA` 和 `PA-DQN` 的奖励都依赖定位误差。
- 这在仿真训练条件下是合理的，但正文还没有把“真值仅用于训练阶段奖励构造”写透。
- 后续建议在方法或实验设置中补一句明确说明。

## 4. 奖励函数参数仍未完整给出

- 正文提到奖励函数中精度项和稳定性项参数分别记为 `k` 和 `\lambda`，但参数表中没有给出对应数值。
- 当前版本未处理这一点。
- 后续需要检查：
  - `k` 是否在公式中实际使用；
  - `\lambda` 的具体数值是否需要补入参数表。

## 5. 投影算子 `\Pi(\cdot)` 仍缺少具体实现说明

- 目前正文只说将中间权重映射到可行域，但没有给出具体投影方式。
- 后续建议至少补一句，例如：
  - 先裁剪负值再归一化；
  - 或明确写为投影到概率单纯形。

## 6. “权重变化率”指标仍未定义 已解决

- 消融实验表中保留了“权重变化率”这一指标，但正文没有说明其计算方式。
- 后续建议补公式或文字定义，例如相邻时刻权重差分范数的平均值。

## 7. 实验细节说明仍不充分

- 当前仍未补充以下内容：
  - 训练集、验证集、测试集划分方式；
  - EKF 的状态模型、观测模型和噪声协方差设置原则；
  - RL-IFA 与 EKF 的参数调优过程；
  - 多次随机实验是否取均值及方差。

## 8. `RSSI-only` 仍未纳入对比或解释缺失

- 第三章融合框架包含 `RSSI` 分支，但总体性能对比表中没有 `RSSI-only`。
- 这不一定是错误，但最好给出说明：
  - 是因为该方法表现过差而省略；
  - 还是实验设计中未单独统计。

## 9. 结果支撑仍可进一步增强

- 当前 `PA-DQN` 相比 `EKF` 和 `RL-IFA` 的提升幅度较大。
- 这本身不构成错误，但后续若时间允许，建议补强：
  - 分轨迹类型结果；
  - 多随机种子重复实验；
  - 均值 ± 标准差；
  - 更完整的消融或敏感性分析。
