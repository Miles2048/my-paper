# 硕士答辩逐页演讲稿 v1

说明：根据当前 `v1答辩ppt.pdf`、`ppt/7.png` 至 `ppt/34.png`、论文正文与答辩大纲整理。前 6 页空白页暂时跳过。本稿按 18-22 分钟答辩节奏设计，核心方法与实验页可适当放慢。

## 第 7 页：系统任务场景与统一观测模型

各位老师，这一页介绍本文后续方法共同依赖的系统任务场景和统一观测模型。本文研究的是异常线索触发后的非合作低轨卫星互联网终端定位排查问题。目标终端数量和位置未知，可以用目标状态集合表示；无人机则在不同空间位置进行机动观测，获取 RSSI、AOA、时间特征以及轨迹递推等多源信息。

从建模角度看，定位系统并不是直接从信号读出位置，而是经过复杂传播环境映射后得到观测集合。论文中用状态方程和观测方程描述这一过程：目标状态随时间演化，观测则由目标状态经过传播环境和测量噪声映射得到。因此，定位误差不仅来自测量噪声，也来自目标状态、传播环境和观测方式的共同作用。这一认识是后续自适应融合和 Radio Map 建模的基础。

## 第 8 页：传播机理与经典定位方法局限

这一页说明为什么传统单一定位方法在本文场景下难以稳定工作。复杂城市环境中存在建筑遮挡、反射、绕射和散射，信号到达无人机时往往不再是理想直达路径，而是由多条传播路径叠加形成。这样会带来多径反射、非视距偏差、阴影衰落和时延扩展。

具体到定位方法，RSSI 方法低成本、易部署，但功率和距离的映射容易被遮挡和阴影衰落破坏；AOA 方法依赖到达方向，但强反射时主径方向可能并不指向真实目标；TOA 和 TDOA 方法依赖稳定时间测量和同步条件，而非合作卫星互联网信号存在时钟漂移和调度跳变；PDR 短时平滑，但长期会累计漂移。因此，单一观测量在复杂环境下并不可靠，后续需要多源融合与智能决策机制。

## 第 9 页：智能决策与知识迁移基础

这一页给出本文两类核心方法的理论基础。左侧是 MDP 和 DQN。对于单目标连续定位，融合权重不是一个固定参数，而是会随着环境状态持续变化。因此，本文将调权过程建模为序列决策问题：智能体根据状态选择动作，动作改变融合权重，定位结果再产生奖励反馈，进而更新策略。

右侧是知识蒸馏。对于多无人机协同定位，实际部署中可能出现节点掉点，导致学生网络只能看到残缺观测。本文利用完整观测下训练得到的 Teacher 网络，将其输出的软标签和全局空间判别知识迁移给 Student 网络。这样，学生网络虽然输入不完整，但在训练时能够学习完整网络对空间区域的判别逻辑，为第四章掉点鲁棒定位提供支撑。

## 第 10 页：单目标连续定位问题

接下来进入第一个核心工作：单目标连续定位。本文考虑的是复杂城市环境下非合作终端的连续轨迹估计。目标终端不提供同步辅助、位置辅助或协议配合，无人机只能依赖被动接收的观测信息进行估计。

论文中首先定义目标位置为 \(p_t=[x_t,y_t]^T\)，第 \(m\) 个无人机观测节点位置为 \(s_{m,t}=[x_{m,t},y_{m,t},z_{m,t}]^T\)，二者之间几何距离为公式中所示的三维欧氏距离。同时，观测模型写为 \(z_{m,t}=h_m(p_t,s_{m,t};\xi_t)+v_{m,t}\)，其中 \(\xi_t\) 表示传播环境因素，\(v_{m,t}\) 表示测量噪声和未建模扰动。这个模型强调：RSSI、AOA 和 PDR 虽然物理含义不同，但都可以被统一看作目标状态在不同观测域下的映射结果。问题的关键在于，这些观测质量会随 LOS/NLOS 切换、多径和运动状态动态变化。

## 第 11 页：单目标 PA-DQN 总体框架

这一页是第三章方法的总体框架。本文提出基于物理感知深度强化学习的单目标自适应融合定位方法，也就是 PA-DQN。整体流程分为三条观测分支和一个智能调权模块。

RSSI、AOA 和 PDR 分支分别输出初步位置估计 \(P_t^{RSSI}\)、\(P_t^{AOA}\) 和 \(P_t^{PDR}\)。与此同时，系统从观测及射线追踪结果中提取 RMS 时延扩展、RSSI 方差、RF-PDR 几何一致性和上一时刻权重，形成物理感知状态 \(S_t\)。PA-DQN 智能体根据状态输出动作，再得到当前融合权重 \(\omega_t\)。最终位置估计由公式给出，即三类位置估计按照动态权重加权求和，并满足三个权重之和为 1。

这个框架的核心不是简单把三个结果平均，而是让智能体根据环境状态动态判断每一路观测在当前时刻是否可信。

## 第 12 页：多源加权融合模型

这一页展开说明三类观测分支和融合公式。RSSI 分支使用带有 NLOS 附加偏差的对数距离路径损耗模型，其中 \(b_{m,t}^{NLOS}\) 表示遮挡和多径带来的附加误差。AOA 分支使用 `atan2` 表示目标相对于无人机的到达方位角，同时考虑反射路径、阵列误差或 NLOS 造成的角度偏差。PDR 分支不直接依赖无线传播，而是根据步长和航向角进行短时位置递推。

三类分支分别对应功率域、几何域和运动域信息。融合时，本文在位置层进行加权，得到 \(P_t^{Fused}\)。这里真正困难的地方不是加权公式本身，而是权重 \(\omega_t\) 如何根据复杂传播环境和观测质量动态确定。因此，下一步就把权重更新建模为强化学习中的序列决策问题。

## 第 13 页：调权问题的 MDP 建模

这一页介绍 PA-DQN 的 MDP 建模。本文将动态融合过程表示为五元组 \(\mathcal{M}=<\mathcal{S},\mathcal{A},\mathcal{P},\mathcal{R},\gamma>\)。其中，状态空间用于描述传播复杂度、观测统计特征、几何一致性和历史权重；动作空间表示融合权重的更新策略；奖励函数评价当前决策对定位精度和轨迹平滑性的影响。

在每个时刻，智能体观测状态 \(S_t\)，选择动作 \(a_t\)，动作进一步改变权重 \(\omega_t\)，系统输出融合定位结果并得到奖励 \(r_t\)，再进入下一状态。为了处理连续高维状态，本文采用 DQN 近似状态-动作价值函数。底部公式给出了 DQN 的目标值和损失函数，其中目标网络参数 \(\theta^-\) 和经验回放机制用于提升训练稳定性。通过这种建模，权重调整从静态参数设定转化为随环境状态演化的决策过程。

## 第 14 页：物理感知状态空间

这一页是 PA-DQN 的关键创新点。传统强化学习融合方法往往只根据后验误差调整权重，也就是误差已经发生后才进行修正。本文希望智能体在误差显著放大之前，就能感知环境退化趋势，因此构造了物理感知状态空间。

状态由四类信息组成。第一是 RMS 时延扩展 \(\sigma_\tau\)，用于表征多径时延离散程度；多径越复杂，时延扩展通常越大。第二是 RSSI 方差 \(\sigma_{RSSI}^2\)，用于刻画局部时间窗口内功率波动性。第三是 RF-PDR 几何一致性 \(D_{RF-PDR}\)，即无线观测位置和 PDR 递推位置之间的欧氏距离，用来反映不同信息源是否相互支持。第四是上一时刻权重 \(\omega_{t-1}\)，用于保持调权过程的时间连续性。

最终状态向量写为 \(S_t=[\sigma_\tau,\sigma_{RSSI}^2,D_{RF-PDR},\omega_{t-1}]^T\)。这个设计使智能体可以从传播、统计、几何和历史决策四个层面判断当前观测可信度。

## 第 15 页：PA-DQN 状态与融合决策关系

这一页进一步说明物理感知状态如何转化为融合决策。左侧输入包括 RMS 时延扩展、RSSI 方差、RF-PDR 一致性和上一时刻权重。PA-DQN 将这些状态输入 Q 网络，对 9 个候选离散增量动作进行价值估计，然后选择 Q 值最大的动作。

动作定义为 \(a_t=[\Delta\omega_t^{RSSI},\Delta\omega_t^{AOA}]^T\)，其中每个增量可以取 \(-\Delta,0,+\Delta\)。PDR 权重由归一化约束得到。随后通过投影算子 \(\Pi(\cdot)\) 保证更新后的权重非负且和为 1。这样，模型不是直接大幅改变权重，而是在上一时刻权重附近进行小步长修正。

因此，PA-DQN 的核心作用可以概括为：根据物理环境和多源一致性主动调节权重，把传统的“误差发生后修正”前移为“环境退化前感知”。

## 第 16 页：离散增量动作与奖励设计

这一页介绍动作空间和奖励函数。动作空间方面，本文没有采用粗粒度权重切换，而是采用离散增量动作。RSSI 和 AOA 两个权重分量各有减小、保持、增加三种选择，因此构成 9 个候选动作。这样既保留了 DQN 适合处理离散动作的优势，又限制了相邻时刻权重突变。

奖励函数方面，本文将精度项和平滑项结合。基础奖励为负的定位误差 \(-\varepsilon_t\)，同时加入权重变化惩罚 \(-\lambda||\omega_t-\omega_{t-1}||_2\)。最终奖励写为 \(r_t=-\varepsilon_t-\lambda||\omega_t-\omega_{t-1}||_2\)。也就是说，智能体不仅要降低当前定位误差，还要避免为了短时误差而频繁剧烈调权。这个设计主要服务于连续轨迹场景，可以抑制局部锯齿和权重震荡。

## 第 17 页：PA-DQN 网络与训练流程

这一页说明 PA-DQN 的网络结构与训练机制。网络输入是物理感知状态，包括 \(\sigma_\tau\)、\(\sigma_{RSSI}^2\)、\(D_{RF-PDR}\) 和 \(\omega_{t-1}\)。这些特征经过嵌入和联合表征后，进入 DQN 决策头，输出各候选动作对应的 Q 值，即 \(Q(S_t,a_i;\theta)\)。

训练时，本文采用经验回放和目标网络机制。经验回放池用于打破样本之间的时序相关性，目标网络用于稳定 Bellman 目标值。底部公式给出了目标值 \(y_t=r_t+\gamma\max_{a'}Q(S_{t+1},a';\theta^-)\)，以及均方误差损失函数。

需要说明的是，本文采用离线训练、在线推理的方式。离线阶段利用仿真平台的真实轨迹计算奖励并训练网络；在线阶段不再需要真实位置，只输入当前状态，快速输出调权动作。

## 第 18 页：单目标实验设置

这一页是单目标实验设置。实验基于复杂城市仿真环境开展，首先利用 OSM 数据构建真实街区三维场景，再通过射线追踪刻画多径、遮挡和 LOS/NLOS 切换。测试轨迹包括矩形、对角线、反对角线和随机游走，总测试里程超过 200 米。

训练配置方面，状态输入就是前面介绍的四类物理感知特征，动作步长设置为 \(\Delta=0.05\)，经验回放池容量为 \(1\times10^5\)，批大小为 64，折扣因子为 0.95，优化器采用 Adam，初始学习率为 \(1\times10^{-3}\)，最大训练轮数为 800。

评价指标主要采用 RMSE 和 P95。RMSE 反映整体定位精度，P95 反映尾部误差和偶发大误差控制能力。实验重点验证 PA-DQN 在复杂环境切换、多类轨迹和低信噪比条件下的精度与鲁棒性。

## 第 19 页：单目标总体性能对比

这一页给出单目标定位的总体性能结果，是第三章最重要的实验页。横向比较的方法包括 PDR-only、AOA-only、EKF、RL-IFA 和本文提出的 PA-DQN。

从表格和柱状图可以看到，PDR-only 和 AOA-only 的误差都比较大，说明单源观测在复杂城市环境下难以稳定支撑连续定位。EKF 相比单源方法有所改善，但仍受固定模型和噪声假设限制。RL-IFA 引入强化学习后进一步降低误差，但由于主要依赖后验误差反馈，提升仍有限。

相比之下，PA-DQN 取得最低 RMSE 和 P95，分别为 2.1468 米和 3.8724 米。这说明引入物理感知状态后，智能体能够更准确识别环境退化并及时调整融合权重，因此在整体误差和尾部误差控制方面均优于对比方法。

## 第 20 页：不同轨迹场景性能

这一页进一步分析不同运动轨迹下的性能表现。论文将测试场景分为矩形轨迹、线性轨迹和随机游走轨迹。其中，线性轨迹主要对应对角线和反对角线穿越，运动方向相对稳定；矩形轨迹包含多次转角；随机游走则方向变化频繁，传播条件切换更复杂。

从结果看，PA-DQN 在三类轨迹下都取得最低 RMSE 和 P95。在线性轨迹下，PA-DQN 的 RMSE 为 1.8765 米，是三类场景中最低的；矩形轨迹中 RMSE 为 2.2846 米；随机游走最具挑战，PA-DQN 的 RMSE 仍控制在 2.5496 米。

这说明本文方法的优势并不依赖某一类固定轨迹，而是在转角、长距离穿越和不规则运动条件下都能保持稳定性能。

## 第 21 页：典型轨迹可视化

这一页从可视化角度展示定位轨迹效果。四个子图分别对应矩形、对角线、反对角线和随机游走轨迹。真实轨迹作为参考，PA-DQN 的输出整体更贴近真实轨迹，而 EKF、RL-IFA 和单源方法在部分区段会出现偏移、折线化或局部抖动。

在矩形轨迹中，转角位置最容易出现误差放大，PA-DQN 的外扩偏离更小。在对角线和反对角线路径中，经过 LOS/NLOS 切换区段后，PA-DQN 能够更快回到真实轨迹附近。在随机游走场景中，目标运动方向频繁变化，PA-DQN 的局部震荡仍然较弱。

这说明本文方法的优势不仅体现在统计指标上，也体现在连续轨迹的可视化平滑性和局部稳定性上。

## 第 22 页：信噪比鲁棒性与消融实验

这一页包含两部分：信噪比鲁棒性和消融实验。左侧结果表明，随着信噪比降低，各方法误差都会上升，但 PA-DQN 的 RMSE 和 P95 曲线增长更平缓，说明其在观测质量下降时仍能保持较好鲁棒性。这与物理感知状态设计一致，因为智能体可以通过时延扩展、RSSI 方差和几何一致性提前感知观测退化。

右侧消融实验进一步分析各模块作用。完整 PA-DQN 的 RMSE 为 2.15 米，权重变化率为 0.04。去除物理感知状态后，RMSE 上升到 4.89 米，说明物理状态是精度提升的主要来源。去除增量动作后，RMSE 为 4.52 米，权重变化率恶化到 0.28，说明动作设计直接影响权重震荡。去除稳定性奖励后，RMSE 变化不大，但权重变化率明显升高，说明平滑奖励主要改善轨迹连续性。

## 第 23 页：单目标方法小结

这一页对第三章进行小结。单目标方法的核心转变，是从传统的“后验误差修正”转向“物理先验感知驱动调权”。传统方法通常在误差已经出现后才调整策略，而且难以区分误差来自 RSSI 波动、AOA 偏置还是 PDR 漂移。本文则把多径复杂度、功率波动和跨源一致性提前引入状态空间，让智能体在误差放大之前进行调权。

实验上，PA-DQN 在总体测试集上 RMSE 达到 2.1468 米，P95 达到 3.8724 米；在低信噪比条件下误差增长更平缓；在连续轨迹上也表现出更好的平滑性。因此，第三章回答的是：在复杂环境中如何稳定融合多个观测量完成单目标连续定位。接下来，第四章进一步扩展到多目标并发定位和残缺观测鲁棒协同问题。

## 第 24 页：多目标并发定位问题

这一页进入第四章，多目标并发定位。与第三章的单目标连续轨迹估计不同，多目标场景不仅要估计位置，还要面对目标数量不确定、空间扫描稀疏和多径鬼影等问题。

论文中用位置集合 \(\mathcal{P}=\{p_1,p_2,\ldots,p_K\}\) 表示区域内多个目标终端，其中每个 \(p_k=[x_k,y_k]^T\)。无人机沿航迹进行空间扫描，只能得到有限离散样本集合 \(\mathcal{D}=\{(s_i,r_i)\}_{i=1}^N\)。每个观测值 \(r_i\) 是多个目标空间响应和噪声的叠加，即公式中的求和形式。

这意味着多目标定位不能简单看作多个单目标问题的叠加。传统峰值搜索容易把噪声伪峰或多径鬼影当成真实目标。因此，本文将问题转化为 Radio Map 上的区域级空间判别。

## 第 25 页：稀疏采样与局部多峰问题

这一页具体说明稀疏采样和局部多峰问题。理想情况下，单一目标的空间响应应该形成一个连续单峰结构。但无人机只能在有限航迹位置采样，再叠加测量噪声和采样不均匀，就可能使原本单峰响应分裂成多个局部高值点。

论文中将扰动后的观测场写为 \(\hat{g}(x)=g(x)+\xi(x)\)，离散样本写为 \(\hat{r}_i=r_i+\delta_i\)。局部极值集合表示为 \(\mathcal{M}=\{m_1,m_2,\ldots,m_L\}\)。当 \(L>1\) 时，峰值数量已经不能直接等同于真实目标数量。

传统峰值判定规则只判断某点是否超过阈值且是否为局部极大值，因此难以区分真实目标、噪声伪峰和多径鬼影。由此引出本文的核心启示：判定单元需要从“点峰值”转向“区域结构”。

## 第 26 页：Radio Map 信号成像思路

这一页介绍 Radio Map 信号成像方法。本文首先将无人机获取的离散 RSSI 样本组织为 \(\mathcal{D}=\{(s_i,r_i)\}_{i=1}^N\)，然后在目标区域上建立规则网格 \(\mathcal{G}\)。通过空间映射、插值和平滑操作，将离散观测转换为定义在网格上的连续场近似 \(\hat{R}(u,v)=\mathcal{F}(\mathcal{D},g_{u,v})\)，最终形成二维 Radio Map 矩阵 \(R\)。

Radio Map 的意义不只是把点补全成图，而是把目标响应从离散数值点提升为具有邻域关系和区域纹理的空间表征。对于真实目标，其响应通常形成连续区域；对于噪声伪峰或多径鬼影，其区域结构和邻域衰减方式往往不同。因此，本文将传统峰值检测转化为 Radio Map 上的区域级判别。

## 第 27 页：OSM + MATLAB 工具链与传播场建模

这一页说明 Radio Map 的物理建模来源。本文不是直接用任意插值图像作为输入，而是基于真实地理信息和射线追踪构建空间传播场。

具体流程是：首先从 OSM 数据中提取建筑轮廓、道路结构和高度信息；然后在 MATLAB Site Viewer 中完成三维城市场景重建和可视化校验；再利用 Antenna Toolbox 配置收发天线与平台位置；最后通过 RF Propagation Toolbox 进行射线追踪传播计算。基于该工具链，论文获得覆盖目标区域 27,546 个网格点的高分辨率传播数据。

这些数据为后续 RSSI 栅格映射和 Radio Map 构建提供了物理一致的输入。因此，Radio Map 的基础是城市几何和传播机理，而不是简单图像化处理。

## 第 28 页：Radio Map 三维可视化

这一页展示复杂传播场下 Radio Map 的三维可视化结果。不同城市场景下，空间功率分布并不是规则、平滑的单峰结构，而会受到建筑遮挡、街道峡谷、反射增强和阴影衰落共同影响。

在城市峡谷场景中，建筑布局会使信号沿街道方向形成非均匀扩散；在反射增强场景中，局部区域可能出现伪热点；在阴影衰落场景中，真实目标附近的响应边界可能被削弱或断裂。这些现象说明，多目标定位中的空间响应具有明显区域纹理。

因此，Radio Map 为后续图像域区域判别提供了更丰富的信息。它能够帮助算法不仅看某个点是否高亮，还能观察高亮区域的范围、形态和邻域结构，从而缓解局部多峰和多径鬼影带来的误判。

## 第 29 页：多无人机掉点与残缺观测

这一页进一步讨论实际协同部署中的节点掉点问题。多无人机协同定位并不总能保持完整拓扑，受续航、链路中断、遮挡或电磁干扰影响，部分无人机可能暂时失效，导致完整协同观测退化为残缺观测。

论文中用 \(\delta=[\delta_1,\delta_2,\ldots,\delta_{N_u}]^T\) 表示节点可用状态，其中 \(\delta_i=1\) 表示节点有效，\(\delta_i=0\) 表示节点掉点。覆盖率 \(\rho_{cov}\) 表示有效节点比例，掉点比例 \(\eta_t=1-N_t^{act}/N\) 表示拓扑退化程度。

需要强调的是，掉点并不是简单少几个测量值，而是会同时造成空间采样密度下降、节点间几何基线削弱，以及 Radio Map 结构失真。因此，需要一种机制把完整观测下学到的判别能力迁移到残缺观测条件下。

## 第 30 页：教师—学生知识蒸馏建模

这一页介绍知识蒸馏建模。对于第 \(i\) 架无人机和目标 \(j\)，节点特征定义为 \(X_{i,j}=[x_i,y_i,z_i,R_{i,j}]\)，即无人机三维位置和对应 RSSI 测量值。Teacher 网络接收完整观测集合，输出全局栅格判别分布 \(\pi^T\)；Student 网络只接收有效节点集合，也就是残缺观测，输出 \(\pi^S\)。

为了迁移教师网络中的判别知识，本文使用温度系数 \(T_d\) 对 logits 进行 softmax，得到软标签分布 \(q_m^\ell\)。蒸馏损失采用 KL 散度，衡量 Teacher 和 Student 输出分布的差异。最终学生网络的训练目标由交叉熵损失和蒸馏损失共同组成，即 \(L=\alpha L_{ce}+\beta L_{kd}\)。

这样，学生网络学习的不只是硬标签对应的单个正确栅格，还包括完整观测下不同栅格之间的相似性和空间判别关系。

## 第 31 页：教师—学生 GAT 网络架构

这一页说明具体网络结构。多无人机观测天然具有图结构，每架无人机对应一个节点，节点特征为 \([x_i,y_i,z_i,R_i]\)。本文采用全连接图建模，使任意两个有效节点之间都可以进行信息传播。

核心特征提取模块是多层图注意力网络，也就是 GAT。图注意力机制首先计算节点 \(i\) 和节点 \(j\) 之间的注意力分数 \(e_{ij}\)，再通过 softmax 得到注意力系数 \(\alpha_{ij}\)，最后对邻居节点特征加权求和得到新的节点表示 \(h'_i\)。经过多层 GAT 后，采用全局平均池化得到图级表示 \(h_G\)，再通过 MLP 分类头输出 400 个栅格类别。

这个结构有三个作用：第一，适配掉点造成的变长节点输入；第二，学习节点间空间相关性；第三，保证 Teacher 和 Student 最终输出维度一致，便于蒸馏训练。

## 第 32 页：多目标实验设置与复杂度

这一页介绍第四章实验设置和复杂度分析。实验区域大小为 500 米乘 500 米，划分为 20 乘 20 个栅格，因此共有 400 个空间类别。协同网络由 16 架无人机构成，掉点比例设置为 0%、12.5%、25%、37.5% 和 50%，分别对应不同程度的残缺观测。

网络方面，Teacher 和 Student 都采用三层 GAT，隐藏维度为 64，注意力头数为 4，分类头结构为 64 到 128 再到 400。数据集按 70%、15%、15% 划分为训练集、验证集和测试集。

复杂度方面，Student-KD 的额外开销主要发生在训练阶段，需要引入 Teacher 网络前向和蒸馏监督；但测试阶段，Student-KD 的推理结构与 Student-NoKD 一致。因此，蒸馏增强不会增加学生模型在线推理复杂度，这一点对低成本无人机平台具有工程意义。

## 第 33 页：25% 掉点条件下性能对比

这一页是第四章最重要的实验结果。实验固定掉点比例为 25%，也就是 16 架无人机中随机缺失 4 架，对比 Teacher、Student-NoKD、Interp-Loc 和 Student-KD。

从误差累计分布可以看到，Teacher 作为完整观测上界，曲线最靠左；Student-NoKD 由于只利用残缺观测训练，误差明显增大；Interp-Loc 通过插值补全有一定改善，但仍没有利用完整网络的全局判别知识。Student-KD 曲线显著靠近 Teacher，并明显优于 Student-NoKD 和 Interp-Loc。

定量结果上，Student-KD 的 Mean Error 为 12.8 米，RMSE 为 16.0 米，90% Error 为 26.4 米；相比 Student-NoKD 的 22.4、27.9 和 46.5 米，约取得 40% 的误差下降。这说明知识蒸馏能够有效迁移完整网络中的全局空间判别能力。

## 第 34 页：总结与展望

最后对全文工作进行总结。本文围绕低成本无人机平台下非合作低轨卫星互联网终端定位问题，主要完成了两方面研究。

第一，针对单目标连续定位中观测质量动态波动的问题，提出 PA-DQN 自适应融合定位方法。该方法将 RSSI、AOA 和 PDR 的融合权重更新建模为序列决策问题，并引入 RMS 时延扩展、RSSI 方差和 RF-PDR 一致性构成物理感知状态。实验结果表明，PA-DQN 在总体测试集上取得 2.1468 米 RMSE 和 3.8724 米 P95。

第二，针对多目标并发定位和节点掉点问题，提出 Radio Map 与知识蒸馏结合的鲁棒协同定位方法。该方法将离散 UAV 扫描观测转化为空间区域表征，并通过 Teacher-Student 蒸馏缓解随机掉点带来的性能退化。

后续工作将从真实场景验证、三维定位扩展、多目标精细判别和动态协同网络四个方向继续推进。总体来看，本文证明了传播机理建模、空间/状态表征、智能学习和鲁棒机制结合，可以有效提升复杂城市环境下低成本无人机平台的定位精度、环境适应性与工程鲁棒性。谢谢各位老师，恳请批评指正。
